# pnpm 데이터는 전부 이 저장소의 .local/ 아래(D 드라이브)에 둔다 — CLAUDE.md 8.1절
# 상대 경로는 이 파일이 있는 디렉토리 기준으로 해석된다. 설치 후 `pnpm store path`로 실제 위치를 확인한다.
store-dir=.local/pnpm-store
cache-dir=.local/pnpm-cache
state-dir=.local/pnpm-state
