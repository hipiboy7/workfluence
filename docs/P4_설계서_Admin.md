# P4_설계서_Admin — 권한 세분화·관리·알림

- 대상 Phase: 4 / 브랜치 `impl-phase4` / 작성일 2026-09-22
- 인수 기준(`docs/scope-definition.md` 5절): **관리자가 사용자·스페이스를 관리하고 감사로그로 추적한다. 삭제한 페이지를 휴지통에서 되살린다. 멘션이 알림함에 뜬다**

> **문서 이름.** 이 Phase는 관리 콘솔·휴지통·알림·운영 정책값 넷을 다룬다. `<Topic>`은 코드 모듈명과 맞추라는 규칙(`CLAUDE.md` 10.3절)에 따라 **가장 큰 덩어리인 `admin`**을 이름으로 쓰고, 나머지는 절로 나눈다. Phase당 설계서는 하나다 (10.1절).

---

# A. 착수 쟁점

`CLAUDE.md` 1.1절 1단계는 사용자가 정할 것을 대화로 묻게 되어 있다. **이번에는 묻지 않았다** —
사용자가 "phase5까지 제한없이 물어보지 말고 진행"(2026-09-22)이라고 지시했기 때문이다.
그래서 아래는 **Claude가 정한 것이고, 각 항목에 그렇게 정한 근거와 되돌릴 조건을 적는다.**
사용자가 다르게 보면 그 항목만 뒤집으면 된다.

| # | 쟁점 | 정한 것 | 근거 · 되돌릴 조건 |
|---|---|---|---|
| 1 | 알림을 어디까지 | **앱 안 알림함만.** 메일·사내 메신저는 하지 않는다 | `scope-definition` 4.2절이 외부 알림을 Phase 6으로 미뤄 뒀다. 사내 메일 릴레이 정보가 확인되지 않았다(확인 필요 B 계열). 요구가 오면 `NotificationChannel` 인터페이스 뒤에 붙인다 |
| 2 | 무엇이 알림을 만드나 | **멘션(`@아이디`)만.** 댓글과 페이지 본문 둘 다 | 인수 기준이 "멘션이 알림함에 뜬다"다. "내 스페이스에 새 글" 류는 요구가 확인되지 않았고, 켜는 순간 알림이 소음이 된다 |
| 3 | 운영 정책값을 언제 DB로 | **이번에 옮긴다.** 업로드 상한·확장자·세션 타임아웃·비밀번호 정책·감사로그 보존 | `CLAUDE.md` 5절이 "Phase 4부터 DB `settings` + 관리 화면"으로 이미 정해 뒀다. 미루면 5절이 거짓이 된다 |
| 4 | 페이지 단위 세밀 권한 | **하지 않는다** | `scope-definition` 4.2절이 "스페이스 단위로 시작해 **실제 요구를 본 뒤** 넓힌다"로 두었고, 그 요구가 확인되지 않았다. 요구 없이 넓히면 판정 코드가 두 배가 되고 되돌리기 어렵다 |
| 5 | 휴지통의 범위 | **페이지·첨부·댓글·스페이스 전부.** 되살리기는 페이지·스페이스만 화면에 둔다 | 네 가지 모두 `deleted_at`을 이미 갖고 있다. 첨부·댓글은 부모를 되살리면 함께 보이므로 따로 화면을 두지 않는다 |
| 6 | 물리 삭제 배치 | **명령만 만든다** (`pnpm trash:purge`). 자동 실행은 하지 않는다 | `scope-definition` 8절 위험 7이 "Phase 4에서 정리 배치를 **설계**한다"다. 자동 실행은 운영 환경을 알아야 정할 수 있어 Phase 5 배포가이드로 넘긴다 |
| 7 | 라벨 화면 | **넣는다** (Phase 3 인계 1항) | 테이블만 있고 호출자가 없다. 보류 10("호출자 없는 코드")을 닫는 자리다 |

---

# B. Confluence 대조 (기능 Phase 필수, `CLAUDE.md` 4절)

| Confluence의 것 | 우리 | 왜 |
|---|---|---|
| 페이지별 보기/편집 제한(상속·예외) | **제외** | 쟁점 4. 300명 규모에서 스페이스 단위로 충분하다는 것이 아직 반증되지 않았다 |
| 공간 관리자(Space Admin) 별도 역할 | **변형 — Crew `owner`가 그 일을 한다** | 역할을 늘리면 판정 조합이 늘어난다. `owner`가 이미 Crew 관리·상태 변경을 한다 |
| 워치(Watch) — 페이지 변경 구독 | **제외** | 쟁점 2. 요구가 확인되지 않았고 알림 소음의 주된 원인이다 |
| 휴지통 | **채택** | 인수 기준 |
| 멘션 알림 | **채택, 앱 안에서만** | 쟁점 1 |
| 라벨 | **채택 (단순형)** | 색·계층 없이 이름만. 요구가 확인된 만큼만 |
| 감사로그 보존 정책 | **채택 — 설정값 + 정리 명령** | 위험 7 |

---

# C. 요구사항

## 알림 (FR-500 ~ FR-509)

| # | 요구 | 근거 |
|---|---|---|
| FR-500 | 댓글·페이지 본문에서 `@아이디`를 찾아 **알림을 만든다** | 인수 기준 |
| FR-501 | 멘션 추출은 A등급 순수 함수다. 문서 JSON에서 텍스트를 뽑아 판정한다 | `CLAUDE.md` 3절 |
| FR-502 | **볼 수 없는 사람에게는 알림을 만들지 않는다.** 스페이스 판정을 통과한 사용자만 | 알림이 존재를 알려 주는 통로가 되면 안 된다 |
| FR-503 | 자기 자신을 멘션하면 알림을 만들지 않는다 | |
| FR-504 | 같은 저장에서 같은 사람을 여러 번 적어도 알림은 하나다 | |
| FR-505 | 알림함에서 읽음 처리한다. 안 읽은 수를 화면 머리말에 보여 준다 | 인수 기준 |
| FR-506 | 알림은 **파생 데이터가 아니다.** 지운 댓글의 알림은 남되 링크는 대상이 없음을 알린다 | 지워졌다고 "누가 나를 불렀다"가 사라지면 안 된다 |
| FR-507 | 알림 생성·읽음은 감사로그에 남기지 **않는다** | 검색과 같은 이유 — 양이 불어나고 추적 가치가 낮다 |
| FR-508 | 알림 목록은 자기 것만 본다. 남의 알림은 조회 경로가 없다 | |
| FR-509 | 보내는 채널은 `NotificationChannel` 인터페이스 뒤에 둔다. 구현은 앱 안 저장뿐 | 쟁점 1의 되돌릴 자리 |

## 휴지통 (FR-510 ~ FR-517)

| # | 요구 | 근거 |
|---|---|---|
| FR-510 | 지운 페이지를 목록으로 보고 **되살린다** | 인수 기준 |
| FR-511 | 되살리기 권한은 그 스페이스의 쓰기 권한을 따른다 | 기존 판정 재사용 |
| FR-512 | 부모가 지워진 페이지를 되살리면 **스페이스 최상위로 올린다** | 없는 부모를 가리키면 트리에서 사라진다 |
| FR-513 | 지운 스페이스는 관리자만 보고 되살린다 | `space.manage` |
| FR-514 | 되살리기를 감사로그에 남긴다 | `CLAUDE.md` 6절 |
| FR-515 | 보존 기간(기본 30일)을 넘긴 것은 `pnpm trash:purge`가 **물리 삭제**한다 | 위험 7 |
| FR-516 | 물리 삭제도 감사로그에 남긴다. 무엇을 몇 건 지웠는지 | |
| FR-517 | 첨부 파일 실체는 **참조가 하나도 안 남을 때만** 지운다 | Phase 3 인계 7항 — 같은 내용을 여러 메타데이터가 참조한다 |
| FR-540 | 감사로그도 보존 기간이 지나면 지운다 (`pnpm audit:purge`). **append-only 트리거는 그대로 두고 좁은 예외만 연다** | 위험 7. "아무도 못 지운다"와 "보존 기간이 지나면 지운다"를 같이 지키려면 **정해진 방법으로만** 지울 수 있어야 한다. 예외 조건 둘(이 트랜잭션이 명시 + 기준 시각 이전)을 모두 만족해야 하고, UPDATE는 어떤 경우에도 막는다 |

## 운영 정책값 (FR-520 ~ FR-528)

| # | 요구 | 근거 |
|---|---|---|
| FR-520 | 정책값을 DB `settings`에 두고 관리 화면에서 바꾼다 | `CLAUDE.md` 5절 |
| FR-521 | 대상: 업로드 상한·허용 확장자·세션 타임아웃·비밀번호 정책·감사로그 보존 기간·휴지통 보존 기간 | 5절 세 번째 분류 |
| FR-522 | **기본값은 `constants`에 남는다.** DB에 값이 없으면 기본값을 쓴다 | 5절. DB가 비어도 기동해야 한다 |
| FR-523 | 읽기는 캐시하고, 바꾸면 **즉시 반영**한다 | "바꿨는데 재기동해야 먹는다"는 조용히 잘못되는 유형이다 |
| FR-524 | 값 검증은 A등급 순수 함수다. 범위를 벗어나면 400 | 상한을 0으로 두면 아무도 못 올린다 |
| FR-525 | 정책값 변경을 감사로그에 남긴다. **바뀐 키와 이전·이후 값** | `CLAUDE.md` 6절 |
| FR-526 | 변경은 `settings.manage` 권한자만 | |
| FR-527 | `WF_UPLOAD_MAX_MB` 등 기존 환경변수는 **DB에 값이 없을 때의 기본값**이 된다 | 키를 지우면 운영 `.env`가 깨진다 |
| FR-528 | **업로드 상한만은 환경변수가 천장이다.** DB로 그보다 크게 올리면 400 | multer 방벽이 기동 시점에 정해져, 더 크게 올려도 요청이 그 앞에서 잘린다. "바꿨는데 안 먹는" 상태가 되느니 못 올리게 막는다 |

## 관리 콘솔·라벨 (FR-530 ~ FR-539)

| # | 요구 | 근거 |
|---|---|---|
| FR-530 | 관리자가 **모든 스페이스**를 보고 상태를 바꾼다 | 인수 기준. `space.manage`는 Phase 2에 이미 있다 |
| FR-531 | 감사로그를 행위·행위자·기간으로 거른다 | "추적한다"는 거를 수 있어야 성립한다 |
| FR-532 | 카테고리를 관리자가 이름 변경·삭제한다. 쓰는 스페이스가 있으면 삭제 거부 | `scope-definition` 7.2절의 "남용되면 Phase 4" |
| FR-533 | 라벨을 페이지에 붙이고 뗀다. 이름은 유일 | Phase 3 인계 1항 |
| FR-534 | 라벨로 페이지를 찾는다 | 붙이기만 하고 못 찾으면 쓸모가 없다 |
| FR-535 | 라벨 권한은 그 페이지의 스페이스 판정을 따른다 | |
| FR-536 | 라벨 작업을 감사로그에 남긴다 | |
| FR-537 | 관리 화면의 모든 목록은 페이지네이션 대신 **상한 + 검색**이다 | 300명 규모에서 목록이 수천을 넘지 않는다. 넘으면 그때 만든다 |
| FR-538 | 카테고리 삭제는 **쓰는 스페이스가 있으면 거부**한다 | 조용히 `NULL`로 만들면 그 스페이스들이 어느 분류였는지 복구할 수 없다 |
| FR-539 | 관리자가 **다른 사용자의 세션을 강제 종료**한다. 서버측 세션을 파기한다 | `scope-definition` 4.1절 #4가 Phase 4로 둔 것. 쿠키만 지우게 하면 훔친 세션이 산다 (FR-224와 같은 판단) |

## 비기능 (NFR-40 ~ NFR-43)

| # | 목표 |
|---|---|
| NFR-40 | 정책값 변경이 **재기동 없이** 다음 요청부터 반영된다 |
| NFR-41 | 알림 생성이 저장 응답을 느리게 하지 않는다 — 같은 트랜잭션이되 질의 수가 멘션 수에 비례한다 |
| NFR-42 | 휴지통 목록 p95 < 300ms (스페이스당 1,000건 기준) |
| NFR-43 | A등급 커버리지 ≥ 90%, api ≥ 70% |

---

# D. 데이터 모델 (신규)

`docs/설계서_Architecture.md` 3.1절이 단일 출처다. 이번 Phase가 더하는 것:

| 테이블 | 무엇 | 비고 |
|---|---|---|
| `notifications` | 받는 사람·종류·대상(page/comment)·만든 사람·읽은 시각 | FR-500~509 |
| `settings` | **이미 있다.** 키별 JSON 값 + 바꾼 사람·시각 | FR-520 |

`labels`·`page_labels`는 Phase 3에서 만들었다. 이번에 호출부가 붙는다.

---

# D.1 API 계약

`CLAUDE.md` 4절 1단계가 요구하는 절이다. **Phase 3 설계서가 이 절을 빠뜨렸고**, 그때
자체 점검이 "Phase 4 설계서부터 절 구성을 점검 목록으로 만든다"고 했다 (P3 검토서 #7).

권한 열의 "스페이스 판정"은 그 페이지가 속한 스페이스의 `spaceAccess()` 결과를 뜻한다.
새 판정을 만들지 않는다.

| 메서드·경로 | 권한 | 돌려주는 것 |
|---|---|---|
| `GET /api/notifications?limit=` | 로그인. **자기 것만** | `NotificationView[]` |
| `GET /api/notifications/unread-count` | 로그인 | `{ count }` |
| `POST /api/notifications/:id/read` | 자기 알림만 | `{ ok }`. 남의 것이면 404 |
| `POST /api/notifications/read-all` | 로그인 | `{ count }` |
| `GET /api/trash/pages?limit=` | 스페이스 판정 `canWrite` | `TrashPageView[]` |
| `POST /api/trash/pages/:id/restore` | 같음 | `{ ok, movedToRoot }` |
| `GET /api/trash/spaces?limit=` | `space.manage` | `TrashSpaceView[]` |
| `POST /api/trash/spaces/:id/restore` | `space.manage` | `{ ok }` |
| `GET /api/settings/policy` | 로그인. **관리자가 아니면 일부만** | `Policy`(부분) + `uploadCeilingMb` |
| `PATCH /api/settings/policy` | `settings.manage` | `{ ok }` |
| `GET /api/labels?limit=` | 로그인 | `LabelView[]` |
| `GET /api/labels/:name/pages?limit=` | 질의에서 거름 | `SearchHit[]` |
| `GET /api/pages/:id/labels` | 스페이스 판정 `canRead` | `LabelView[]` |
| `POST /api/pages/:id/labels` | `canWrite` | `LabelView` |
| `DELETE /api/pages/:id/labels/:labelId` | `canWrite` | `{ ok }` |
| `GET /api/audit?limit=&action=&actorId=&from=&to=` | `audit.read` | `AuditEventView[]` (Phase 11부터 `requestId`로도 거르고 행에 요청 번호 — P11 F절) |
| `PATCH /api/categories/:id` | `space.manage` | `CategoryView` |
| `DELETE /api/categories/:id` | `space.manage` | `{ ok }`. 쓰는 스페이스가 있으면 409 |
| `POST /api/users/:id/terminate-sessions` | `user.manage` | `{ count }` |

**상태 코드의 뜻을 고정한다.** 못 보는 것은 **404**다 (403이 아니다 — 403은 "그것이 있다"를
알려 준다). 권한은 있는데 상태가 막는 것은 403. 규칙 위반은 400, 크기 초과는 413,
충돌은 409.

# D.2 화면 흐름

```
첫 화면(스페이스 목록)
├─ 알림 (n)  →  /notifications   읽음 처리 · 모두 읽음 · 대상으로 이동
├─ 휴지통    →  /trash           지운 페이지 되살리기 / (관리자) 지운 스페이스
├─ 검색      →  /search
└─ (관리자) 운영 설정 → /admin/policy   숫자 값 · 허용 확장자
                사용자 관리 → /admin/users    승인 · 잠금 해제 · 초기화 · 역할 · **세션 강제 종료**
                감사로그    → /admin/audit    행위 · 기간으로 거르기

페이지 보기 /pages/:id
├─ 라벨      붙이기 · 떼기 · 라벨 이름 누르면 /labels/:name
├─ 첨부
└─ 댓글      @아이디로 부르면 그 사람 알림함에 뜬다
```

**되살리기가 부모를 잃었을 때는 말해 준다.** 맨 위로 옮겼다는 안내를 띄우지 않으면
사용자는 되살린 문서를 찾지 못한다 (FR-512).

---

# E. 모듈과 등급

> **경로에 백틱이 없는 줄은 아직 만들지 않은 것이다.** 만드는 커밋에서 백틱을 붙인다 —
> `pnpm verify:docs`가 "백틱 경로는 저장소에 있어야 한다"를 검사하므로(`CLAUDE.md` 4.1절),
> 미리 붙이면 설계서를 쓰는 순간 검사가 깨진다.

| 모듈 | 경로 | 등급 | 하는 일 |
|---|---|---|---|
| 멘션 추출 | `apps/api/src/notifications/domain/mention.ts` | **A (신규)** | 문서 JSON에서 `@아이디`를 뽑는다. 순수 함수 |
| 정책값 검증 | `packages/shared/src/policy.ts` | **A (신규)** | 범위·형식 판정. 서버·화면이 같이 쓴다 |
| 알림 | `apps/api/src/notifications/` | B | 생성·목록·읽음. 채널 경계 |
| 휴지통 | `apps/api/src/trash/` | B | 목록·되살리기·물리 삭제 |
| 정책값 | `apps/api/src/settings/` | B | 읽기 캐시·변경·감사 |
| 라벨 | `apps/api/src/labels/` | B | 붙이기·떼기·찾기 |
| 화면 | `apps/web/src/pages/` (알림함·휴지통·라벨 찾기) · `apps/web/src/pages/admin/` (정책 설정) · `apps/web/src/components/` (라벨 붙이기) | B | |
| E2E | `e2e/admin.spec.ts` | C | 멘션 → 알림 → 휴지통 복원 → 정책 변경 |

---

# F. 설정 항목 (신규)

| 키 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `WF_TRASH_RETENTION_DAYS` | int | `30` | 휴지통 보존 기간. DB `settings`에 값이 있으면 그쪽이 이긴다 (FR-527) |
| `WF_AUDIT_RETENTION_DAYS` | int | `365` | 감사로그 보존 기간. 같은 규칙 |

`.env.example`·`deploy/compose.yml`을 같은 커밋에서 갱신한다 (`CLAUDE.md` 5절).

---

# G. 구현 순서

1. 마이그레이션(`notifications`) + `schema.ts`
2. 설정 키 2개 + `.env.example` + compose
3. **A등급 둘 — 테스트 먼저**: 멘션 추출, 정책값 검증
4. 정책값 서비스(캐시·변경·감사) + 소비 지점 연결
5. 알림 (생성 훅을 댓글·페이지 저장에 끼운다)
6. 휴지통 + `pnpm trash:purge`
7. 라벨
8. 화면 · E2E
9. 검증기록 → 자체 점검 → 가이드 갱신

---

# G.1 이전 Phase가 넘긴 것 — 받거나 다시 미루거나

Phase 2·3이 "Phase 4에서"라고 적어 둔 항목들이다. **설계서가 받지 않으면 Phase가 닫힐 때
조용히 사라진다.** 각각 이번에 할지 말지를 정한다.

| 출처 | 항목 | 이번 Phase |
|---|---|---|
| P3 #1 | 라벨 화면·API | **했다** (FR-533~536) |
| P3 #4 | 운영 조절값을 DB `settings` + 관리 화면으로 | **했다** (FR-520~528) |
| P2 #5 | 페이지 이동 **화면**(끌어 옮기기) | **안 한다.** 인수 기준이 요구하지 않고, 트리 UI는 그 자체로 한 Phase 분량이다. `기능백로그`로 넘긴다 |
| P2 #6 / P2 자체 7 | `page_versions`에 append-only 트리거 | ~~안 한다~~ → **Phase 5에서 했다** (`0006_constraints`). UPDATE만 막고 DELETE는 두었다 — 정리 배치가 버전을 먼저 지우기 때문이다 |
| P2 #4 | `kind`·`status`에 DB CHECK 제약 | ~~안 한다~~ → **Phase 5에서 했다** (`0006_constraints`). `users.role`·`users.status`·`spaces.kind`·`spaces.status`·`space_members.role` |
| P2 #7 / 자체 10 | 개인 스페이스 개수 제한 | **안 한다.** 화면에 경로가 없고 실제 피해가 확인되지 않았다. 요구가 오면 백로그로 |
| P2 자체 11 | `page.restore` 상수에 호출자가 없다 | **정리했다.** 휴지통이 `page.restore.trash`를 쓰므로 이름이 갈렸다. 둘 다 남긴다 — 하나는 버전 복원, 하나는 휴지통 복원으로 **뜻이 다르다** |
| P2 자체 13 | 분류 중복 생성이 409가 아니라 201(멱등) | ~~Phase 5~~ → **Phase 5에서 했다.** `P2_설계서_Page`의 FR-308 문구를 코드에 맞췄다 |
| P2 자체 18 | 홈이 `scope=all`을 안 불러 admin 목록이 불완전 | **안 한다.** 관리자 전용 전체 목록 화면 대신 감사로그·스페이스 상태 변경으로 갈음했다. 필요해지면 백로그로 |
| P3 #5 | `client_max_body_size`와 업로드 상한의 기계 검사 | ~~안 한다~~ → **Phase 5에서 했다.** `pnpm verify:docs`가 `deploy/nginx.conf`와 `.env.example`을 대조한다 |
| P3 #6 | 재색인 구현이 둘 | ~~안 한다~~ → **Phase 5에서 했다.** 질의와 범위를 `apps/api/src/pages/reindex.ts` 한 곳으로 모았다. 실행 방법은 여전히 둘이다 |
| P3 자체 13·15 | `page.delete` 조건이 공허한 것, viewer 댓글 금지가 설계서에 없는 것 | **권한 세분화를 하지 않기로 했으므로**(쟁점 4) "그때 살아난다"는 근거가 사라졌다. 지금 규칙("그 스페이스에 쓸 수 있으면")이 **최종이다.** P3 설계서의 FR-422 문구는 그 Phase의 기록이므로 고치지 않고, 최종 규칙을 여기 적는다 (10.3절) |

# H. Phase 5 인계 (착수 시점 예상)

- 물리 삭제 배치의 **자동 실행**은 Phase 5 배포가이드에서 정한다 (쟁점 6).
- 외부 알림 채널은 Phase 6 (쟁점 1).
