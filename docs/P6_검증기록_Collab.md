# P6_검증기록_Collab — 실시간 편집·비교·내보내기·템플릿·메일 검증

작성 2026-09-22. 브랜치 `impl-phase6`. 설계서는 [`docs/P6_설계서_Collab.md`](P6_설계서_Collab.md).

> 시행착오의 경위는 여기 쓰지 않는다 (4절). `docs/internal/검토서_트러블슈팅.md`를 가리킨다.

## 실행 환경

| 항목 | 값 |
|---|---|
| 호스트 | 사내 Linux 서버 (RHEL 9), 개발·빌드 공용 |
| Node / pnpm | v24.21.0 / 12.4.1 |
| PostgreSQL | 임베디드 17.10 (개발·테스트 분리) |
| 디스크 | 여유 **11GB** |
| app 이미지 | **390MB** (NFR-64 예산 400MB — 여유 10MB) |
| git | `ca00fa4` |

---

## 1. 테스트 건수와 커버리지

명령: `pnpm test:cov` (종료 코드 0). 수치는 `coverage-summary.json`에서 뽑았다.

| 패키지 | 테스트 | skip | 라인 | 브랜치 | 함수 | 관문 |
|---|---|---|---|---|---|---|
| `packages/shared` (A) | 159건 | 0 | **99.30%** | **96.96%** | 98.66% | ≥90% 파일 단위 |
| `apps/api` (A+B) | 360건 | 0 | **82.24%** | **74.84%** | 81.62% | ≥70% 패키지 합계 |
| E2E | **19건** | 0 | — | — | — | 측정 제외 |

A+B 가중 평균(라인) **87.09%** (관문 80%).

### 1.1 Phase 6이 만든 파일

| 파일 | 라인 | 브랜치 | 등급 |
|---|---|---|---|
| `packages/shared/src/diff.ts` | 100% | 100% | A |
| `packages/shared/src/html.ts` | 100% | 90.38% | A |
| `apps/api/src/pages/domain/realtime.ts` | 100% | 96.96% | A |
| `apps/api/src/pages/domain/ydoc.ts` | 100% | 95.45% | A |
| `apps/api/src/pages/collab/session-auth.ts` | 100% | 100% | B |
| `apps/api/src/templates/templates.service.ts` | 100% | 84.21% | B |
| `apps/api/src/mail/http.sender.ts` | 100% | 90% | B |
| `apps/api/src/mail/mock.sender.ts` | 100% | 100% | B |
| **`apps/api/src/pages/collab/collab.gateway.ts`** | **0%** | 0% | B |
| **`apps/api/src/mail/mention-mail.service.ts`** | **0%** | 0% | B |

**0%인 둘을 그대로 적는다.** 둘 다 동작은 확인했지만(3·7절) **단위 테스트가 없다.**

- 게이트웨이는 WebSocket 업그레이드·중계·타이머가 얽혀 있어, 판정 로직을 A등급
  `realtime.ts`로 **빼낸 뒤 남은 것이 배선**이다. 그 배선은 실제 연결로 확인했다.
- 멘션 메일 서비스는 `MailSender`(100%)를 부르는 얇은 층이다.

그래도 **커버리지 0은 0이다.** 다음에 이 둘을 고칠 때 테스트를 먼저 만든다.

### 1.2 관문이 잡은 것

`updateTemplateDto`의 "빈 몸통 거부" 분기에 테스트가 없어 shared의 `perFile` 관문이
빨개졌다. Phase 5에서 켠 것이 또 한 번 값을 했다.

새 B등급 파일 넷이 테스트 없이 들어왔을 때 api가 94.55% → **78.47%**로 떨어졌다.
관문(70%)은 넘었지만 **떨어진 것 자체가 신호**라 보고 채웠다 (→ 82.24%).

---

## 2. 보류 결정 판정

| # | 항목 | 판정 |
|---|---|---|
| 4 | 실시간 편집 저장 모델 | **닫음. JSON이 정본, Yjs 상태는 `page_realtime`의 파생 데이터** (설계서 C.1절) |
| 7 | PDF 내보내기 | **닫음. 하지 않는다** (사용자 결정). HTML + 인쇄 CSS |
| 17 | `drizzle/meta` 스냅샷 | **닫음. `pnpm db:generate`를 쓰지 않는다** — 3절 |
| 신규 18 | 사내 메일 API 실연동 | 등재. 모의 통과는 완료가 아니다 |

Confluence 가져오기는 `scope-definition` 4.2절에서 **하지 않는 것으로 닫았다** (사용자 결정).

---

## 3. 보류 17 — `db:generate`를 실제로 돌려 봤다

```
$ pnpm db:generate
[✓] Your SQL migration file ➜ drizzle/0008_lying_power_pack.sql 🚀
```

**오류 없이 성공했다.** 그런데 생성된 파일이 `page_realtime`·`page_templates`를
**CREATE TABLE로 다시 만든다** — 손으로 쓴 `0007_collab`이 이미 만든 표다.
스냅샷 사슬이 `0004`에서 멈춰 있어 drizzle이 0005~0007을 모르기 때문이다.

그대로 적용하면 "이미 있는 관계"로 죽는다. **조용히 틀리는 쪽이 아니라 시끄럽게
죽는 쪽이라 그나마 낫지만**, 생성된 파일을 그대로 믿으면 안 된다는 것은 같다.

생성된 것은 지웠고 journal도 되돌렸다. **마이그레이션은 계속 손으로 쓴다** —
스냅샷 사슬을 다시 세우려면 손으로 쓴 SQL(트리거·CHECK·GUC 예외)을 drizzle 스키마로
옮겨야 하는데, 그것들은 애초에 drizzle이 표현하지 못해서 손으로 쓴 것이다.

---

## 4. 실시간 동시 편집 (FR-700~712)

### 4.1 인증이 실제로 막는다 (FR-703)

`ws://…/api/ws/pages/<id>`에 붙어 봤다.

| 시도 | 결과 |
|---|---|
| 쿠키 없음 | **HTTP 401** |
| 가짜 서명 (`s:fake.badsignature`) | **HTTP 401** |
| 서명 없는 sid | **HTTP 401** |
| uuid가 아닌 경로 | 연결 거부 (우리 처리기가 손대지 않는다) |

**실패 사유를 구분해 주지 않는다.** 권한 없음과 페이지 없음을 나누면 그것만으로
"그 페이지가 있다"가 새어 나간다 (7절 계정 열거 방지와 같은 판단).

### 4.2 두 클라이언트로 끝까지 (FR-700·701·706·707)

```
준비 완료 — 페이지 750f2e9a…, 현재 버전 1
A: 연결됨 / B: 연결됨
B가 A의 글을 본다: YES      A가 B의 글을 본다: YES      둘 다 남는다: YES
저장된 버전: 2 (처음 1)     정본에 A의 글: YES  정본에 B의 글: YES
가만히 둔 뒤 버전: 2 (안 늘었다 — 정상)
```

마지막 줄이 FR-707이다. 커서만 움직여도 Yjs 변경이 오므로 이것이 없으면
**가만히 보고만 있어도 버전이 쌓인다.**

### 4.3 브라우저 둘로 (E2E)

`e2e/collab.spec.ts` — 서로의 입력이 보이고, 이름이 **두 곳**(커서 이름표·"같이 보는
사람")에 나오고, 편집을 멈추면 버전이 남는 것까지 확인한다.

---

## 5. 버전 비교 (FR-720~726) · HTML 내보내기 (FR-730~737)

```
비교 200 · 변경 1 · 추가 0 · 삭제 0
낱말 차이: removed:처음 removed:<b>내용</b> added:고친 added:내용 added:하나

내보내기 200 · Content-Disposition: attachment; filename*=UTF-8''…
본문 꺾쇠 이스케이프 YES · 제목 이스케이프 YES · 인쇄 CSS YES · 외부 자원 없음 YES
버전을 주면 그 버전, 안 주면 최신 · 없는 버전은 404
감사로그에 page.export 기록됨
```

**`attachment`로 내려보낸다.** 브라우저가 열어 버리면 내보낸 문서가 우리 오리진에서
실행되는 셈이고, 그러면 본문이 우리 CSP 안에서 도는 길이 생긴다.

---

## 6. 템플릿 (FR-740~746)

```
관리자 생성 201 · 같은 이름 다시 201 (같은 id — 멱등)
일반 사용자 생성 403 · 목록 200 · 검증 실패 문서 400
일반 사용자 삭제 403 · 관리자 삭제 200
감사로그: template.create, template.delete
```

멱등 호출은 감사로그에 남기지 않는다 — 남기면 "만들었다"로 가득 차는데 바뀐 것은 없다.

---

## 7. 메일 (FR-750~757)

```
감사로그: mail.send, page.update, page.create, …
모의 발송 — 받는이 ***@example.internal · 제목 "[위키] … 님이 회원님을 불렀습니다"
```

받는 사람 주소는 **도메인만 남기고 가린다** (7절 로그 규칙). 감사로그에는 **받은
사람 수만** 적는다 — 감사로그는 오래 남고 주소는 개인정보다.

**트랜잭션 밖에서 보낸다** (FR-754). 안에서 보내면 연결을 쥔 채 남의 서버를 기다리고
(T-026과 같은 모양), 롤백되면 없던 일에 대한 메일이 나간다.

---

## 8. Phase 2의 인수 기준 하나가 바뀌었다

**이 Phase가 이전 Phase의 인수 기준을 바꾼 유일한 곳이라 따로 적는다.**

Phase 2의 "두 사람이 같은 버전을 저장하면 뒤가 **화면에서** 충돌 안내를 받는다"는
실시간 편집에서 **일어나지 않는다** — 병합되므로 충돌할 것이 없다.

`CLAUDE.md` 0.1절이 애초에 그렇게 적어 뒀다: "실시간 동시 편집은 Phase 6.
**그 전에는** 편집 잠금 + 저장 시 버전 충돌 감지."

FR-709는 REST 저장 경로와 409를 **그대로 두라**고 하고, 그것은 여전히 사실이다.
`e2e/space-page.spec.ts`를 **API의 409를 직접 확인하는 것**으로 바꿨다 —
같은 `baseVersionNo`로 두 번 저장하면 두 번째가 409이고 `currentVersionNo`가 3이다.

---

## 9. 확인하지 못한 것

| 확인 못 한 것 | 왜 |
|---|---|
| **사내 메일 API 실연동** | 개발 서버가 그 API에 나갈 수 있는지 모른다. 요청 형식도 가정이다 (보류 18) |
| **컨테이너 스택에서의 실시간 편집** | 개발 서버(:3000)에서만 확인했다. nginx는 Phase 0부터 `Upgrade`를 넘기게 돼 있지만 **실제로 그 경로로 WebSocket을 연결해 보지는 않았다** |
| **동시 편집자 5명** (NFR-61) | 둘까지만 확인했다 |
| **입력 반영 1초 이내** (NFR-60) | 체감으로는 즉시지만 **재지 않았다** |
| **`collab.gateway.ts`·`mention-mail.service.ts`의 단위 테스트** | 1.1절 |
| **실시간 상태가 커진 경우** | 상한을 두지 않았다. **넘었을 때 무엇을 할지 정하지 못했기 때문이다** — "저장하고 새로 시작"은 그 순간 남의 편집을 끊는다는 뜻이고 그 값어치를 재 본 적이 없다 (보류 19) |

---

## 10. 검증기록을 쓰다 갚은 빚 둘

검증기록을 쓰면서 **내가 만든 빚 둘을 발견해 그 자리에서 갚았다.** 미루려다 말았다 —
둘 다 "다음에"가 아니라 지금 고치는 것이 싼 것이었다.

| 빚 | 무엇이 문제였나 | 어떻게 했나 |
|---|---|---|
| `WF_COLLAB_MAX_STATE_KB` | 설계서 설정 표에 적고 **코드가 쓰지 않았다** (5절 위반) | 표에서 뺐다. **넘었을 때 무엇을 할지 정하지 못한 것**이 진짜 이유다 — 보류 19로 등재 |
| `purgeOrphanState` | 함수는 있고 **호출부가 없었다** (3절 호출자 없는 코드) | 함수를 지우고 `pnpm trash:purge`에 SQL 한 줄로 넣었다. 정리 명령이 이미 고아를 치우는 곳이다 |

두 번째가 더 나은 결말이다. **호출부를 붙이는 대신 함수를 지웠다** — 정리 명령은
이미 라벨 고아를 치우고 있었고, 거기 한 줄을 더하는 것이 클래스에 메서드를 두고
밖에서 부르는 것보다 단순하다.

## 11. 다음으로 넘기는 것

| 항목 | 왜 미루나 |
|---|---|
| 컨테이너 스택에서의 WebSocket 확인 | 반입 전에 반드시 한다 |
| NFR-60·61 측정 | `pnpm load:test`에 실시간 편집 시나리오를 더해야 한다 (보류 14와 함께) |

---

## 12. 자체 점검·리뷰

[`docs/internal/P6_검토서_SelfReview.md`](internal/P6_검토서_SelfReview.md).
