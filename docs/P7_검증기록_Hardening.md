# P7 검증기록 — Hardening

- 작성일: 2026-09-23 / 작성 LLM: Claude Opus 5
- 설계: [`docs/P7_설계서_Hardening.md`](P7_설계서_Hardening.md) · 검토: [`docs/internal/P7_검토서_Review.md`](internal/P7_검토서_Review.md)
- 시행착오는 여기 쓰지 않는다 — [`docs/internal/검토서_트러블슈팅.md`](internal/검토서_트러블슈팅.md) T-032

## 1. 실행 환경

| 항목 | 값 |
|---|---|
| 호스트 | 사내 Linux 서버 (RHEL 9). 개발·빌드 같은 호스트 |
| Node / pnpm | v24.21.0 / 12.4.1 |
| PostgreSQL | 17.10 (임베디드, `.local/pgdata`) · 컨테이너 postgres:17 |
| Docker / Compose | 29.6.1 / v5.3.1 |
| 디스크 | 빌드 전 `/` 여유 10GB |

## 2. 실측 — "상태 벡터로 판정하면 안 된다"

작성자 오염(FR-802)을 고치는 방법으로 **① 적용 전후의 상태 벡터 비교**와 **② Yjs의 `update`
이벤트**를 놓고 재 봤다. ①이 간단해 보였다.

```
빈 문서 전체상태 적용: update이벤트 0회 상태벡터변화=false
이미 아는 상태 되돌려받음: update이벤트 0회 상태벡터변화=false
글자 추가:            update이벤트 1회 origin=["member-A"] 상태벡터변화=true
순수 삭제:            update이벤트 1회 origin=["member-A"] 상태벡터변화=false   ← 여기
```

**①을 골랐으면 "지우기만 한 편집"이 저장되지 않았다.** 상태 벡터는 만들어진 항목의 시계만
담고 삭제는 담지 않는다. ②로 갔다.

앞의 두 줄이 문제의 원인이기도 하다 — 화면은 접속 직후 자기 빈 문서를 한 번 보내는데, 서버가
**`MSG_UPDATE`를 받았다는 사실만으로** 작성자를 바꾸고 있었다.

## 3. 자동 검사

```
$ pnpm check          # lint + typecheck + test + verify:docs
```

| 항목 | 값 | 비고 |
|---|---|---|
| shared 테스트 | **171건** (Phase 6 종료 시 159건) | +12 |
| api 테스트 | **425건** (Phase 6 종료 시 364건) | +61 |
| E2E | **19건 전부 통과** (45.3초) | 비교 단언을 실제 낱말로 바꾼 뒤에도 통과 |
| skip | **0건** | |
| `verify:docs` | 통과 | |

### 커버리지 (`pnpm test:cov`, 관문 통과 — 종료 코드 0)

| 대상 | 라인 | 브랜치 | 함수 | 문장 |
|---|---|---|---|---|
| `packages/shared` (A) | **99.31%** | 97.10% | 98.76% | 99.03% |
| `apps/api` 전체 (B, 관문 70%) | **89.21%** | 79.10% | 88.23% | 85.52% |

이번에 만지거나 만든 파일:

| 파일 | 등급 | 라인 | 브랜치 |
|---|---|---|---|
| `apps/api/src/pages/domain/liveness.ts` | A | **100%** | **100%** |
| `apps/api/src/auth/domain/display-name.ts` | A | **100%** | **100%** |
| `apps/api/src/pages/domain/realtime.ts` | A | 100% | 91.83% |
| `apps/api/src/pages/domain/ydoc.ts` | A | 100% | 96.29% |
| `apps/api/src/common/revocation.bus.ts` | B | 100% | 100% |
| `apps/api/src/pages/collab/collab.gateway.ts` | B | **73.30%** | 63.85% |

> **게이트웨이는 Phase 6에서 0%였다.** 저장소에서 상태가 가장 복잡한 파일인데 테스트가 한 건도
> 없었고, 그 사이에 결함 여섯이 들어와 있었다 (`P7_검토서_Review` §2). 가짜 소켓으로 방의
> 상태 기계를 돌리는 통합 테스트 23건을 더했다. 브랜치 64%가 남은 것은 오류 처리 가지가
> 많기 때문이고, **그중 실제로 도는 것(퇴장 저장 실패·정본 앞섬·권한 회수)은 테스트가 덮는다.**

## 4. 실제로 동작시켜 확인한 것

### 4.1 컨테이너 + nginx 경유 WebSocket (TLS 검증 **켠 채**)

`NODE_EXTRA_CA_CERTS=deploy/certs/cert.pem`로 사내 CA를 신뢰시키고 `rejectUnauthorized`는
건드리지 않았다 (7절).

```
로그인 OK
페이지 28e30185-dbff-4216-8f7d-65143e65b18a
✅ 같은 Origin — 연결됨
✅ 초기 상태 수신 55바이트
— 다른 Origin: 거부 401
— Origin 없음: 거부 401
```

`https://evil.example.internal`을 `Origin`으로 보내면 401, 아예 없어도 401이다 (FR-806).

### 4.2 하트비트와 즉시 파기

```
✅ 연결됨. 35초 동안 하트비트를 본다 (WF_COLLAB_PING_MS 기본 30000)
  ping 1회 (02:49:49)
✅ 하트비트 1회 — nginx proxy_read_timeout 300s보다 자주 온다
강제 종료를 부른다 (user=5cb80026-…)
  응답 201
✅ 편집 연결이 즉시 끊겼다 — code 1008 "세션 파기"
```

**관리자가 강제 종료를 누른 그 순간 편집 연결이 끊겼다.** 주기를 기다리지 않는다 (FR-805).

### 4.2-1 로그아웃은 **그 세션만** 끊는다 (FR-813)

같은 사람이 두 브라우저로 로그인해 둘 다 편집 연결을 연 뒤, 한쪽만 로그아웃했다.

```
✅ 세션 A 연결됨
✅ 세션 B 연결됨
A 로그아웃 응답 201
✅ A의 편집 연결이 즉시 끊겼다 — code 1008 "세션 파기"
✅ B의 편집 연결은 그대로다 — 다른 기기를 끊지 않는다
```

**로그아웃은 버스를 울리지 않고 있었다** (보안 검토 F1). 그래서 로그아웃한 사람의 다른 탭이
최대 5분을 더 살았다. 고치면서 **버스가 세션 id를 나르게** 했다 — 비밀번호 변경·강제 종료는
`sid` 없이 불러 전부 끊고, 로그아웃은 누른 세션 하나만 끊는다.

### 4.3 혼자 오래 열어 두기

nginx `proxy_read_timeout 300s`가 끊는지 본다. **혼자 있고 아무것도 치지 않는다** — 여럿이면
서로의 글자가 오가서 이 시험이 성립하지 않는다.

```
✅ 390초(6.5분) 동안 혼자 열어 두었는데 끊기지 않았다. 서버 ping 13회
```

390초 / 30초 ≈ 13이라 ping이 빠짐없이 갔다. **하트비트가 없던 Phase 6에서는 300초에 끊겼다** —
편집 화면에 "연결이 끊겼다"가 뜨고, 그 뒤에 쓴 것은 아무에게도 가지 않았다.

### 4.4 통합 테스트가 실제로 확인한 것 (요약)

| 확인 | 결과 |
|---|---|
| 고친 뒤 마지막 사람이 나가면 버전이 하나 남는다 | 통과 |
| **접속만 하고 아무것도 안 고치면 버전이 안 생긴다** | 통과 |
| **접속만으로 작성자가 바뀌지 않는다** (FR-802) | 통과 |
| 페이지가 사라진 채 마지막 사람이 나가도 **처리되지 않은 거부가 없다** | 통과 |
| 세션 삭제 / 계정 비활성 / 쓰기 권한 상실 → 다음 재판정에 끊는다 | 통과 (각각 1008 + 이유) |
| 문제가 없으면 안 끊는다 | 통과 |
| 세션 파기 버스는 즉시, 남의 파기에는 안 끊긴다 | 통과 |
| pong이 없으면 다음 주기에 `terminate` | 통과 |
| `flush`가 **저장이 끝난 뒤** 돌아온다 | 통과 |
| 본문이 같아도 **제목이 바뀌면 남는다** | 통과 |
| 내용을 비운 채 마지막 사람이 나가도 **빈 버전이 안 덮인다** | 통과 |

## 5. 이미지 크기 (FR-804)

```
$ docker compose -f deploy/compose.yml --env-file deploy/.env build api
$ docker images --filter=reference='workfluence*'
workfluence-app:latest  379MB
```

| 시점 | 크기 | 예산 400MB 대비 |
|---|---|---|
| Phase 5 종료 | 389MB | 여유 11MB |
| Phase 6 종료 | **400MB** | **여유 0** |
| Phase 7 | **379MB** (`workfluence-app:<sha>-p7`) | **여유 21MB** |

지운 것은 런타임 `node_modules`의 **소스맵과 README**다. 실측으로 소스맵 9.8MB(2,445개) ·
문서 1.7MB(266개)였고, 실제로는 21MB가 줄었다(계층 압축 차이). `LICENSE`는 남겼고
(7절 라이선스 목록), 우리 `dist`의 소스맵도 남겼다 — 장애 때 필요하다.

> **`-name '*.md'`만 썼으면 `LICENSE.md`도 지워졌다.** 주석은 "`LICENSE`는 남긴다"고
> 적었는데 실제로 `jose`·`ms` 같은 운영 의존성이 그 이름을 쓴다 (P7 보안 검토 F5).
> 고친 뒤 이미지 안에서 세어 확인했다 — 라이선스 파일 **158개**, README **0개**, `/app` **45MB**.

## 5.1 두 번째 검토 묶음

**병합 전에 검토 셋을 다시 돌렸다** — 이 Phase 자신에 대해서다. 세 번 다 새 결함이 나왔고,
그중 **높음 셋은 내가 이번에 고치면서 만든 것**이다. 결과와 처리는
[`docs/internal/P7_검토서_Review.md`](internal/P7_검토서_Review.md) 두 번째 묶음에 있다.

| 검토 | 결과 |
|---|---|
| `self-reviewer` | 13건 (높음 3·보통 5·낮음 5) |
| 보안 검토 | 5건 (MEDIUM 4·LOW 1) |
| `doc-consistency` | 22건 |

## 6. 보류 결정 처리

| # | 처리 |
|---|---|
| 20 | **닫는다.** ① 주기 재판정 + 즉시 통지 ② 30초 ping/pong ③ 작성자는 실제 변경자 ④ `taskList` 제거. 넷 다 실측으로 확인했다 |
| 신규 21 | **협업 저장의 멘션 귀속** — 알림함의 "누가 불렀다"가 마지막 타이핑한 사람으로 남는다. 유실은 막았고 메일은 이름을 빼서 틀리지 않는다 |

## 7. 확인하지 못한 것

- **실연동은 여전히 없다.** 사내 IdP(보류 11)·사내 메일(보류 18) 둘 다 모의로만 확인했다.
- **재판정 주기의 실제 지연을 운영에서 재지 않았다.** 기본 5분이고, 그 5분은 "Crew에서 빠진
  사람이 계속 쓸 수 있는 최대 시간"이다. 세션을 끊는 동작은 즉시라 여기 해당하지 않는다.
- **동시 편집자를 많이 놓고 재지 않았다.** 매초 DB 질의를 없앤 것(코드 리뷰 13)의 효과를
  숫자로 보여 주지 못한다. 보류 14(쓰기·고장 상황 측정)에 함께 둔다.
- **`taskList` 제거가 기존 문서를 거부하는지**는 개발 DB 25건에서 0건임을 확인한 것이 전부다.
  운영 데이터는 아직 없다(반입 전).
- **A등급 모듈의 테스트 선행을 커밋 이력으로 증명하지 못한다** (3절·1.5절 3단계).
  이 Phase가 한 커밋이다. 되돌릴 수 없으므로 사실대로 적는다.
- **앞단에 장비가 하나 더 있는 구성을 확인하지 못했다.** `Origin` 검사는 호스트만 보므로
  그 구성에서도 동작해야 하지만, 실제로 그런 배치를 만들어 보지는 못했다.
- 6.5분 유지 시험은 **이 호스트의 nginx 설정**(`proxy_read_timeout 300s`)에 대한 것이다.
  폐쇄망의 앞단에 다른 장비가 있으면 그쪽 유휴 타임아웃을 따로 봐야 한다.
