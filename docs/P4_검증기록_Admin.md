# P4_검증기록_Admin — 권한 세분화·관리·알림 검증

> **이 기록은 미완성이다.** Phase 4는 **끝나지 않았다** — 사용량 상한($500)에 가까워
> 작업을 멈췄기 때문이다. 아래는 **멈춘 시점까지 실제로 측정한 것**이고, 남은 것은 6절에
> 있다. 설계는 [`docs/P4_설계서_Admin.md`](P4_설계서_Admin.md)에 있다.
> 시행착오의 경위는 [`docs/internal/검토서_트러블슈팅.md`](internal/검토서_트러블슈팅.md) T-022·T-023·T-024다.

| 항목 | 값 |
|---|---|
| 일자 | 2026-09-22 |
| 브랜치 | `impl-phase4` (main에 **병합하지 않았다**) |
| 환경 | Linux 공유 서버, Node v24.21.0, pnpm 12.4.1, 임베디드 PostgreSQL 17.10 |

---

## 1. 테스트 건수와 커버리지

`pnpm check` → 종료 코드 **0**.

| 대상 | 파일 | 건수 | skip |
|---|---|---|---|
| `packages/shared` | 6 | 71 | 0 |
| `apps/api` | 27 | 273 | 0 |
| E2E (`pnpm test:e2e`) | 5 | 17 | 0 |

이번 Phase가 만든 모듈 (`pnpm test:cov`, v8):

| 파일 | 등급 | 라인 | 브랜치 | 함수 |
|---|---|---|---|---|
| `apps/api/src/notifications/domain/mention.ts` | **A** | 100.00 | 100.00 | 100.00 |
| `packages/shared/src/policy.ts` | **A** | 100.00 | 100.00 | 100.00 |
> 이 행은 **한 번 틀리게 적었다.** 처음에 100/100으로 썼는데 실측은 브랜치 89.28%였고,
> A등급 기준(90%) 미달인데 패키지 합계가 가려 관문은 초록이었다. 자체 점검이 잡았다
> (`internal/P4_검토서_SelfReview.md` 1절). 빈 분기에 테스트를 넣어 지금은 표대로다.
| `apps/api/src/notifications/notifications.service.ts` | B | 100.00 | 93.75 | 100.00 |
| `apps/api/src/audit/audit.service.ts` | B | 100.00 | 96.55 | 100.00 |
| `apps/api/src/trash/trash.service.ts` | B | 100.00 | 87.50 | 100.00 |
| `apps/api/src/settings/settings.service.ts` | B | 100.00 | 100.00 | 100.00 |
| `apps/api/src/labels/labels.service.ts` | B | 100.00 | 81.81 | 100.00 |

전체: `apps/api` 라인 **93.80%** · 브랜치 82.89%, `packages/shared` 라인 **96.35%** · 브랜치 94.97%.
`apps/web`은 테스트 0건·커버리지 0%다 (`CLAUDE.md` 3절 — web은 관문 없이 측정·기록만).

> **이 표는 손으로 쓰지 않았다.** `coverage-summary.json`에서 뽑아 넣었다 — 한 번 손으로
> 옮기다 틀렸기 때문이다 (아래).

> **이번에도 측정 대상이 틀려 있었다** (T-024). Phase 3의 T-020과 **글자 그대로 같은 증상**이다.
> T-020의 재발 방지가 "목록에 디렉토리를 더한다 + 주석"이었는데, 주석은 그 파일을 열었을 때만
> 읽히고 새 모듈을 만들 때는 그 파일을 열지 않는다. 그래서 한 Phase 만에 재발했다.
> 이번에는 **방향을 뒤집었다** — 전부 넣고 뺄 것만 이름으로 뺀다. 빠뜨릴 목록이 없어졌다.

---

## 2. 인수 기준 검증 (E2E, `pnpm test:e2e`)

인수 기준: **"관리자가 사용자·스페이스를 관리하고 감사로그로 추적한다. 삭제한 페이지를 휴지통에서 되살린다. 멘션이 알림함에 뜬다"**

```
Running 16 tests using 1 worker
  ✓ e2e/admin.spec.ts:30  › 멘션 → 알림함 → 휴지통 복원 → 라벨
  ✓ e2e/admin.spec.ts:93  › 운영 설정을 바꾸면 다시 띄우지 않아도 먹는다 (NFR-40)
  ✓ e2e/admin.spec.ts:116 › 일반 사용자에게는 운영 설정이 보이지 않는다
  16 passed (14.5s)
```

브라우저에서 확인한 것: 댓글로 동료를 부르면 그 사람 알림함에 뜬다 → 지운 페이지가 휴지통에
있고 되살리면 다시 열린다 → 라벨을 붙이고 라벨로 찾는다 → 관리자가 정책값을 바꾸면
**다시 띄우지 않고** 바로 먹는다 → 일반 사용자에게는 운영 설정 메뉴가 없다.

**대상이 지워진 알림**은 "(지워진 글)"로 뜬다 (FR-506) — 알림은 남되 갈 곳이 없음을 말한다.

---

## 2.2 컨테이너 검증 (2026-09-22)

반입 형상 그대로 확인했다: 이미지 빌드 → 마이그레이션 → nginx TLS를 통한 실호출.

```
docker compose build api            → EXIT=0, workfluence-app:latest 389MB
run --rm api node dist/db/migrate.js → [migrate] 6개 마이그레이션 적용 상태
GET  /api/notifications/unread-count → 200 {"count":0}
GET  /api/trash/pages?limit=10       → 200 0건
GET  /api/settings/policy            → 200 uploadMaxMb 20 · 천장 20 · 키 11개
GET  /api/audit?action=auth.login.success → 200 5건, 전부 auth.login.success
psql: DELETE FROM audit_events       → ERROR: audit_events는 append-only다 (시도: DELETE)
```

마지막 줄이 중요하다 — **append-only 예외는 정해진 절차 밖에서는 열리지 않는다.**
컨테이너 안에서 DB에 직접 붙어 지우려 해도 막힌다.

## 2.1 비기능 실측

| 목표 | 결과 |
|---|---|
| NFR-42 — 휴지통 목록 p95 < 300ms (스페이스당 1,000건) | **p50 3.0ms · p95 3.4ms · max 3.9ms** (20회). 충족 |

합성 페이지 1,000건을 한 스페이스에 넣고 `listPages`를 20회 불렀다. 측정은 테스트
안에서 하므로 `pnpm test`를 돌릴 때마다 다시 재진다 — 느려지면 그때 드러난다.

여유가 큰 이유는 권한 필터가 **질의 안에** 있어 가져오는 행이 상한(200)으로 잘리기
때문이다. 가져와서 걸렀다면 1,000건을 다 읽었을 것이다.

## 3. 규칙 실호출 확인 (통합 테스트)

| 규칙 | 확인 | 결과 |
|---|---|---|
| 볼 수 없는 사람은 못 부른다 (FR-502) | Crew가 아닌 사람을 멘션 | 알림 0건 |
| 관리자는 Crew가 아니어도 받는다 | admin을 멘션 | 알림 1건 — 판정을 `spaceAccess()` 한 곳이 하는 증거 |
| **페이지 본문의 멘션** (FR-500) | 본문에 `@아이디`를 적고 저장 | 알림 1건. 생성·수정 둘 다 |
| 자기 자신·중복 (FR-503·504) | `@me`, `@x @x @x` | 각각 0건·1건 |
| 남의 알림 (FR-508) | 남의 알림 id로 읽음 처리 | "찾을 수 없다" — 있는지도 알려 주지 않는다 |
| 부모가 지워진 페이지 복원 (FR-512) | 부모·자식을 둘 다 지우고 자식만 복원 | `movedToRoot: true`, `parentId: null` |
| 되살리기는 쓰기 권한 (FR-511) | viewer로 시도 | 목록에도 없고 "쓸 권한이 없다" |
| DB 한 줄이 망가져도 기동 | `settings`에 `uploadMaxMb: 'big'` | 기본값으로 떨어지고 예외 없음 |
| 업로드 천장 (FR-528) | 천장 20MB에 50MB 요청 | 400 + 이유 |
| 정책 변경 즉시 반영 (NFR-40) | 바꾼 뒤 재기동 없이 읽기 | 바뀐 값 |
| 잠금 임계가 정책값을 쓴다 | 로그인 실패 누적 | `afterFailure`가 살아 있는 값을 받는다 |
| **비밀번호 강도가 정책값을 쓴다 — 양쪽 다** | 최소 길이 20으로 올리기 / 문자 종류 1로 낮추기 | 올리면 8자가 막히고, 낮추면 1종이 통과한다. 계약(zod)은 바닥(8자)만 본다 |
| 세션 유휴·절대가 정책값을 쓴다 | 요청마다 가드가 읽는다 | 쿠키 `maxAge`를 **요청마다 다시 얹는다** — 안 그러면 기동 시점 값이 굳는다 |
| 라벨 정규화 | `' Report '` / `'report'` | 같은 라벨 |
| 라벨로 찾기 권한 | 남의 팀 페이지에 같은 라벨 | 내 것만 |
| 로그인 제한 (T-023) | 같은 IP에서 성공 로그인 10회 (상한 3) | 전부 통과. 실패는 그대로 막힌다 |

---

## 4. 이번 Phase가 드러낸 결함 셋

세 가지 모두 **테스트가 아니라 다른 작업을 하다가** 드러났다. 기록해 둔다.

| # | 무엇 | 왜 중요한가 |
|---|---|---|
| T-022 | `labels`가 테스트 정리에서 빠져 있었다 | Phase 3에 만들고 Phase 4에 처음 썼다. **표를 만든 커밋과 깨지는 커밋이 한 Phase 떨어져 있다** |
| T-023 | 로그인 제한이 성공까지 세어 정상 사용자를 막는다 | **운영에서 먼저 터졌을 결함**이다 — 사내 NAT 한 주소로 300명이 나간다. E2E가 그 환경을 우연히 재현했다 |
| T-024 | 새 모듈이 또 커버리지 밖 | T-020의 재발 방지가 규칙·주석이어서 한 Phase 만에 재발했다. 구조로 바꿨다 |

---

## 5. 확인하지 못한 것

| 것 | 왜 |
|---|---|
| `pnpm trash:purge`의 실제 삭제 | 보존 기간을 넘긴 데이터가 없어 **0건 경로만** 확인했다. 실제로 지우는 경로는 미확인 |
| 첨부 파일 실체 삭제 (FR-517) | 같은 이유 |
| 감사로그 보존 정리 | `auditRetentionDays` 값만 있고 정리 명령이 없다 |
| `apps/web` 커버리지 | 테스트 0건·0%. `CLAUDE.md` 3절은 web을 "측정·기록만"이라 하는데 기록을 빠뜨렸다 |
| — | 1.1절 5단계의 자체 점검 **넷을 모두 돌렸다** (7절·`internal/P4_검토서_SelfReview.md`) |

---

## 6. 남은 일 (Phase 4를 끝내려면)

1. **관리 콘솔의 나머지** — FR-530(모든 스페이스 관리)·FR-531(감사로그 거르기)·FR-532(카테고리 관리)가 미착수.
2. **감사로그 보존 정리 명령**.
3. **컨테이너 검증** — 빌드·마이그레이션·실호출.
4. **이전 Phase가 "Phase 4에서"로 넘긴 항목 10여 건** — P2·P3 검증기록과 SelfReview에 있는데 이 Phase 설계서가 받지 않았다. 받거나 다시 미루거나 **하나를 골라야** 사라지지 않는다 (7절).
5. **병합** — PR + `--no-ff`.

> **이후 진행** — 사용자가 상한을 $1,000으로 올려(2026-09-22) 작업을 이어갔다.
> 위 목록 1~7 중 **1·2·6·7을 끝냈고**, 남은 것은 아래와 같다.
>
> **이 Phase는 한 번 여기서 멈췄었다.** 사용량 상한($500)에 가까워졌기 때문이고, 기능이 막혀서가
> 아니다. 멈춘 시점의 상태는 `pnpm check` 종료 코드 0, E2E 16건 통과, 모든 작업이
> `impl-phase4`에 커밋·푸시된 상태다. **`main`은 Phase 3까지다.**

> 1.1절 6·7단계(학습가이드·운영가이드)는 **했다** — 지금 동작하는 것까지만 적고 진행 중임을 밝혔다.

---

## 7. 문서 정합성 검토 결과 (2026-09-22)

`doc-consistency`가 **36건**을 냈다. 이번 Phase가 만든 어긋남과 **이전 Phase부터 방치된 것**이 섞여 있다.

**고친 것 (12건)** — 단일 출처가 틀린 것과 이번 Phase가 만든 것을 먼저 고쳤다.
`CLAUDE.md` 4.1절 `trash:purge`·6절 감사 대상·보류 7 이정표·**보류 10 종료 표기**,
`설계서_Architecture` 3.1절 `notifications` 컬럼·2절 코드 트리(없는 `system/` 제거,
새 모듈 셋 추가)·`policy.ts`·스크립트 목록, `README` 스크립트 목록,
운영가이드 7.5절(복원이 이미 있다)·7.9절(확장자 목록을 코드 파일이 아니라 운영 설정에서
본다)·첨부 정리(Phase 5 → `trash:purge`), 이 문서의 자기모순(5절과 6절이 가이드 갱신을
두고 반대말을 했다).

**고치지 않고 남긴 것 (24건)** — 아래는 **Phase 4를 닫기 전에 판단해야 한다.**

| 무엇 | 왜 남겼나 |
|---|---|
| ~~템플릿·HTML 내보내기~~ | **범위 문서에 반영했다** — Phase 6으로 미루고, 그 사실을 표에 적었다. 미루는 것을 적어 두지 않으면 사라진다 |
| ~~세션 강제 종료~~ | **반영했다** — 아직 없다는 것과 Phase 4 남은 일이라는 것을 표에 적었다 |
| ~~권한 세분화~~ | **반영했다** — 재검토를 끝내고 "하지 않는다"로 닫았다 |
| ~~알림 범위~~ | **반영했다** — 멘션만이라고 적었다 |
| 이전 Phase가 "Phase 4에서"로 넘긴 항목 10여 건 | P2·P3 검증기록과 SelfReview의 인계가 P4 설계서에 채택도 재연기도 안 됐다. **설계서에 없으면 Phase가 닫힐 때 조용히 사라진다** |
| 트러블슈팅 색인이 T-017에서 끝난다 | 색인은 "이 오류 본 적 있나"의 입구다. T-018~T-024가 본문에만 있다 |
| P1 설계서의 세션·비밀번호 서술이 "환경변수"로 남아 있다 | 이제 DB 정책값이 우선한다. **과거 Phase 설계서를 어디까지 갱신할지**가 먼저 정해져야 한다 (10.3절은 제자리 개정을 말하지만 범위를 정하지 않았다) |
| 로그인 제한 환불에 FR 번호가 없다 | T-023과 운영가이드에만 근거가 있다. 설계 고리가 빈다 |
| P4 설계서에 **API 계약·화면 흐름 절**이 없다 | `CLAUDE.md` 4절 1단계가 요구하고, P3 자체 점검이 "Phase 4 설계서부터 절 구성을 점검 목록으로"라고 약속했는데 지키지 못했다 |
