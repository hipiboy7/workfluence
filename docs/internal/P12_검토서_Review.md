# P12 검토서 — 답을 기다리는 표시·문서 모양의 한계의 병합 전 검토와 그 처리

- 작성일: 2026-09-26 / 작성 LLM: Claude Opus 5.5
- 대상: `main...impl-phase12` — 병합 전에 돌렸다 (CLAUDE.md 13절). HEAD `01f6809`
- 반영: Red `09e69aa` → Green `99bbfab` · 문서는 이 검토서와 같은 커밋

## 1. 무엇을 돌렸나

| 검토 | 결과 | 오탐 |
|---|---|---|
| `self-reviewer` | 5건 (보통 1 · 낮음 4). 넷은 코드 리뷰와 겹쳤다 — 독립으로 같은 것을 찾았다 | 0 |
| 코드 리뷰 (`/code-review high`, PR #11) | 10건 | 0 |
| 보안 검토 (`/security-review` 지침, `general-purpose`) | 중간 1 · 낮음 1 (확신 8) + 문턱 아래 2. 둘 다 happy-dom의 실제 편집기로 재현했다 | 0 |

## 2. 가장 값있던 것

### ① 보류 25를 닫은 근거가 틀렸다 — 남의 글째 지워지고 받은 사람 이름으로 저장된다 (보안 검토 1)

설계서 C.2의 실측은 어긴 요소 안에 **조작한 사람의 글만** 넣었다. 그래서 "남의 글은 지워지지 않는다"로 읽고 관문에서 막지 않았다.
보안 검토는 **남의 목록 항목**(남의 문단과 중첩 목록)에서 첫 문단만 지우거나 그 앞에 인용 하나를 끼웠다 — 관문은 둘 다 받았고, 받는 편집기가
그 항목을 **통째로** 지웠다. 그 삭제는 받은 사람의 연결에서 나가 자동 저장의 `updatedBy`와 감사 `page.collab.save`가 **받은 사람**이 된다.
한 글자를 지운 사람이 아니라 큰 목록을 지운 사람이 피해자로 남는다 — 금융 감사에서 무거운 일이다.

→ 관문이 목록 항목의 첫 자식을 **적용한 뒤로** 본다(목록 구조를 바꾸는 변경만 적용해 본다 — 처음에는 변경마다 서버 문서를 복제했고, 그 비용이 편집 이력에 비례한다는 것을 종료 루틴 자체 점검이 찾았다(§5)). 비는
것은 막지 않는다(정상 동시 편집에서도 나오고, 지워지는 것은 이미 비운 요소다). 설계서 A.1-3·C.2·D.2, 보류 25의 닫음 사유를 고쳤다.

### ② 새 값 규칙이 표 편집 자체를 막았다 (코드 리뷰 1·2 · 보안 검토 2)

`colwidth`의 0을 받지 않았는데 prosemirror-tables는 "너비 없음"으로 0을 쓴다(열 넣기·표 정리) — 합친 칸 안에 열을 넣는 정상 편집이 끊기고
REST 저장이 400이 될 뻔했다. 붙여 넣기만 줄였을 뿐 표 명령은 거르지 않아, 누가 `colspan` 999를 심어 두면 옆 칸과 합친 동료가 1001을 만들어
끊겼다. → 서버는 0을 받고, 편집기는 범위 밖 값을 만드는 트랜잭션을 하지 않는다(서버와 같은 `nodeAttrProblems`). 편집기로 합치기·열 넣기를 해
본 시험을 두었다.

## 3. 처리

| # | 검토 | 지적 | 처리 |
|---|---|---|---|
| S1 | 보안 (중간) | 남의 목록 항목의 첫 자식을 바꿔 남의 글째 지우게 하고 피해자에게 돌린다 | **반영** — ①. 관문 시험: 앞에 인용 끼우기·첫 문단만 지우기는 받지 않고, 새 항목·항목 지우기·첫 문단 고치기·첫 문단을 새 문단으로 바꾸기는 지난다. 옛 상태에 이미 어긴 항목이 있어도 다른 곳의 변경은 지난다 |
| S2 | 보안 (낮음) | 999를 심은 표에서 합치면 동료가 끊긴다 | **반영** — ②. 편집기 가드를 빼면 합치기 시험이 실패한다 |
| S-후보1 | 보안 | 같은 Y.Doc 재접속의 전체 상태 프레임이 16MiB를 넘을 수 있다 | **문서화** — 설계서 D.5, 보류 19의 판정에 "상태 크기와 프레임 상한"을 더했다. = C5 |
| S-후보2 | 보안 | Phase 12 전의 실시간 상태에 범위 밖 값이 있으면 자동 저장이 막힌다 | **두지 않음** — 개발 DB의 실시간 상태 0건(검증기록 2.4), 운영은 반입 전이다. 막히면 화면이 까닭을 말한다(P9 FR-1011) |
| C1 | 코드 리뷰 | `colwidth` 0을 받지 않는다 | **반영** — ② |
| C2 | 코드 리뷰 | 표 명령이 만드는 값을 줄이지 않는다 | **반영** — ② |
| C3 | 코드 리뷰 | 변환이 떨어뜨리는 요소 안의 멘션을 멘션 자리가 센다 | **반영** — 같은 판정(`keeps`)을 공유한다. 멘션 대조 시험에 떨어지는 요소 둘을 더했다 |
| C4 | 코드 리뷰 | 1009가 감사에 없는데 화면은 "관리자가 감사로그에서 본다"고 한다 | **반영** — `page.collab.reject`(`rule: size`) |
| C5 | 코드 리뷰 | 16MiB가 편집 이력으로 자라는 상태(보류 19)와 이어진다 | **문서화** — = S-후보1 |
| C6 | 코드 리뷰 | 모르는 첫 자식을 두 번 짚는다 | **반영** — 첫 자식이 그 자리에 올 수 없으면 순서 판정은 건너뛴다 |
| C7 | 코드 리뷰 | 문구로 형식을 고른다 | **반영** — 한 모양(`경로: 까닭`), 까닭에 노드 이름을 넣었다 |
| C8 | 코드 리뷰 | JSDoc이 다른 함수 위로 갔다 | **반영** |
| C9 | 코드 리뷰 | 기다리는 동안 화면 전체를 1초마다 다시 그리고, 초를 1초마다 읽어 준다 | **반영** — 기다림 줄만 다시 그리는 작은 부품(`WaitLabel`), 초는 `aria-hidden`, 늦어진다는 알림만 읽힌다 |
| C10 | 코드 리뷰 | 멈추는 중에도 "늦어지고 있다"를 보인다 | **반영** — 멈추는 중에는 숨긴다. 컴포넌트 시험 |

## 4. 자체 점검

검토 도중 Red `09e69aa`가 올라와 트리가 움직였다 — 판정은 커밋 `01f6809` 기준이다.

| # | 심각도 | 지적 | 처리 |
|---|---|---|---|
| R1 | 보통 | 정본은 떨어뜨리는 요소 안의 멘션을 장부가 세어 정본에 없는 사람을 부른 사람으로 알린다 (P8 불변식 "걷는 규칙은 변환과 같다"가 깨짐) | = C3. 반영 |
| R2 | 낮음 | `colwidth` 0 거부 | = C1. 반영 |
| R3 | 낮음 | `ALLOWED_CHILDREN` JSDoc과 시험 설명이 "순서와 개수는 보지 않는다"로 남았다 | **반영** — 두 곳을 고쳤다 |
| R4 | 낮음 | 1009가 감사로그에 없다 | = C4. 반영 |
| R5 | 낮음 | 모르는 첫 자식을 두 번 짚는다 | = C6. 반영 |

확인하고 문제 없다고 한 것: 복원은 검증하지 않아 옛 버전이 400이 되지 않는다 · 변환이 떨어뜨리는 것과 y-tiptap이 지우는 것이 같다 · `ws` 1009는 길이 초과 두 곳뿐 · 기다림 상태 전이 · A등급 Red→Green 이력 · 새 `WF_*` 없음 · 문서 수치(16MiB·1000·10000·5초)가 서로 같다.

## 5. 종료 루틴 (반영분 `01f6809..464dcc0`)

### `doc-consistency` — 15건 + 이번 변경 밖 2건, 오탐 0

반영분이 판단을 뒤집었는데 **옛 판단을 현재로 말하는 문장이 남았다** — 가장 큰 것은 학습가이드 6.12의 "받는 편집기는 그 조각만 지우고
나머지(남의 글)는 그대로 둔다"(필수 갱신 문서). 정당한 과거 서술(정정 이력·실측 표·검토 경위) 20여 곳은 구분해 냈다.

| 무엇 | 처리 |
|---|---|
| 학습가이드 6.12 — 남의 글은 그대로 둔다 · 붙여 넣기만 줄인다 | **반영** — 통째로 지운다 → 서버가 받지 않고 끊는다, 합치기가 되지 않는다, 16MiB 초과는 감사에 남는다 |
| P9 설계서 D.2·D.8 — 순서·개수·프레임을 "보지 않는다" | **반영** — 원문은 두고 "Phase 12가 닫았다"를 괄호로 붙였다 (P9 시점의 판단이다) |
| P12 설계서 A.1-4·FR-1320 — 감사 없음 | **반영** |
| P12 설계서 — 관문의 첫 자식·표 명령 막기에 FR이 없다, 모듈 표에 관문이 없다 | **반영** — FR-1314·FR-1324, 모듈 표에 `gate.ts`(A) |
| P12 설계서 J · 검증기록 6 · Architecture · scope 인수 기준 · CLAUDE.md Phase 표 — 정본만 지킨다로 읽힌다 | **반영** — 관문이 끊는다를 더했다 |
| (밖) scope-definition 상태 "Phase 11까지", 기능백로그 F-005 `진행` | **반영** — Phase 12까지, `완료` |

### `self-reviewer` — 7건 (높음 1 · 보통 1 · 낮음 5), 오탐 0

**반영분이 새로 만든 결함을 찾았다.** 관문이 목록 구조를 바꾸는 변경마다 서버 문서를 통째로 복제했는데, 검증기록은 이력 없는 문서로만 쟀다.
자체 점검이 이력을 쌓아 재 보니 30만 번 고친 문서에서 목록의 Enter 한 번이 1.6초 동안 서버의 모든 방을 멈췄다(18MiB면 6~7초). 고치는
손이 가장 바쁜 자리가 가장 덜 읽힌 자리라는 것(설계서_Agents 17~19)이 또 맞았다.

| # | 심각도 | 지적 | 처리 |
|---|---|---|---|
| R2-1 | 높음 | 흉내 비용이 편집 이력에 비례하고 이벤트 루프를 막는다 | **반영** — 방이 복제본(`Room.scratch`)을 들고 서버 문서의 변경을 따라 받는다. 관문은 거기에 그 변경만 적용한다(0.2ms). 받지 않으면 버리고, 흉내는 다른 판정을 다 지난 뒤에. Red `df2f730` → `0c8b082`. 복제본을 버리지 않으면·흉내를 앞에 두면 시험이 실패하는 것까지 봤다. 만드는 비용(방을 열 때와 같다)은 설계서 D.5·보류 19에 |
| R2-2 | 보통 | 정상 동시 편집이 거절되고, 장애대응이 그것을 "조작"으로 분류한다 | **판단 유지·문서화** — 둘 다 받으면 항목이 통째로 지워져 두 사람의 글이 다 사라진다. 끊으면 한 사람의 마지막 편집만 잃는다. 설계서 A.1-3·D.5에 적고, 장애대응 7.23에 "한 번, 같은 무렵 다른 사람도" 줄을 더했다 |
| R2-3 | 낮음 | 설계서·검증기록은 `role="status"`인데 코드는 아니다 | **반영** — 문서를 코드에 맞췄다. 보다가 **문구까지 `aria-hidden`이라 화면 읽기 사용자는 기다린다는 말을 듣지 못하는 것**도 찾아 초만 뺐다(컴포넌트 시험) |
| R2-4 | 낮음 | 관문이 `'listItem'`을 박아 공유 규칙(`FIRST_CHILD`)이 늘면 관문만 빠진다 | **반영** — `FIRST_CHILD`에서 읽는다. 시험이 그 노드마다 돈다 |
| R2-5 | 낮음 | 멘션 자리(`keeps`)와 변환이 글자 자식을 다르게 센다 | **반영** — `fromYText`의 조각마다 센다 |
| R2-6 | 낮음 | 끊은 연결의 늦은 1009가 그 사람의 거절로 남는다 · 남에게서 온 표 변경을 거르지 않는 것에 시험이 없다 | **반영** — `!member.revoked`, 시험 둘(가드를 빼면 실패) |
| R2-7 | 낮음 | Red `09e69aa`의 실패 수를 확인 못 했다 | **이미 확인** — 메인이 그 커밋의 소스를 꺼내 셌다(검증기록 3절) |
