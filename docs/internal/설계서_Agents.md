# 설계서 — 서브에이전트·스킬 구성

- 작성일: 2026-09-16 (Phase 0)
- 대상: 전 Phase 공용 (`P<phase>` 접두사 없음)
- 정의 파일: `.claude/agents/doc-consistency.md`, `.claude/agents/self-reviewer.md`, `.claude/skills/troubleshoot/SKILL.md`
- 운영 절차: [`CLAUDE.md`](../../CLAUDE.md) 1.1절 8-1 (Phase 종료 루틴), 4.2절 (전체 재독)

> **이 문서가 있는 이유.** 에이전트 정의 파일은 "무엇을 하라"만 담는다. **왜 그 일을 맡기는지, 왜 어떤 일은 맡기지 않는지, 모델을 왜 그렇게 배정했는지**는 어디에도 남지 않는다. 저장소 규칙이 저장소 밖(대화·기억)을 가리키면 다음 담당자가 볼 수 없다.

## 1. 왜 서브에이전트를 쓰나

지금 대화하는 메인 세션을 팀장으로 보면, 팀장이 모든 일을 혼자 할 때 두 가지 문제가 생긴다.

| 문제 | 내용 |
|---|---|
| **책상이 넘친다** | 문서 전체를 읽으면 그 내용이 대화 끝까지 메인 컨텍스트에 남는다. 리뷰 반영마다 반복된다 |
| **작성자가 검토자다** | 자기 실수는 자기 눈에 안 보인다 |

서브에이전트는 **한 가지 일만 맡기는 계약직**이다. 별도 컨텍스트에서 일하고 결과 요약만 돌려준다. 읽은 수천 줄은 그쪽에 남고 메인은 깨끗하다.

**스킬**은 다르다 — 계약직이 아니라 **메인이 따르는 체크리스트**다.

### 1.1 에이전트로 만들 것과 아닌 것

넷 중 **셋 이상**이면 에이전트다.

| 기준 | 뜻 |
|---|---|
| **무거운 읽기** | 파일을 많이 읽어야 하고, 읽은 내용이 결론 도출 후에는 필요 없다 |
| **닫힌 범위** | 입력과 출력이 명확하고 "다 됐다"를 판정할 수 있다 |
| **승인 관문 없음** | 중간에 사용자에게 물어볼 일이 없다 (에이전트는 물어볼 수 없다) |
| **독립된 시각이 결과를 바꾼다** | 작성자가 아닌 눈이 필요한 일 |

## 2. 현재 구성

| 이름 | 형태 | 모델 | 하는 일 | 언제 |
|---|---|---|---|---|
| `doc-consistency` | 에이전트 | fable | 문서 전체를 읽고 변경과 어긋난 문장 목록을 낸다. 읽기 전용 | 리뷰·판단 변경 반영 시, Phase 종료 |
| `self-reviewer` | 에이전트 | fable | 코드·설정·문서의 어긋남과 조용히 잘못되는 결함을 찾는다. 읽기 전용 | Phase 종료 전 |
| `troubleshoot` | 스킬 | (메인) | 문제 해결 직후 트러블슈팅 기록 서식 | 해결 직후 |

## 3. 각각의 근거

### 3.1 `doc-consistency`

| 항목 | 내용 |
|---|---|
| **문제** | 판단 하나를 바꾸면 문서 여러 곳에서 흔적을 찾아 고쳐야 한다. `grep`은 **정확한 글자**만 찾는다 — "12자·3종"을 찾으면 표 안의 `| 비밀번호 | 12자 |`는 놓친다 |
| **왜 에이전트** | 무거운 읽기 ✅ / 닫힌 범위 ✅ (어긋남 목록) / 승인 관문 없음 ✅ / 독립 시각 ✅ — **4/4** |
| **읽기 전용인 이유** | 고치는 판단은 메인이 한다. 에이전트가 고치면 "정당한 이력"을 오탐으로 지울 위험이 있다 |
| **모델** | ~~sonnet — 가장 자주 돌고 결과가 목록이라 메인이 보완할 수 있으니 비용을 아낄 자리~~ → **fable로 승격 (2026-09-17)**. 세 번째 실행에서 놓침이 확인됐다. 사유는 6절 3행 |
| **한계** | 미묘한 모순을 약한 모델이 놓칠 수 있다. **처음 2~3회는 메인이 결과를 독립 재검토**해 놓침이 있으면 모델을 올린다 |

### 3.2 `self-reviewer`

| 항목 | 내용 |
|---|---|
| **문제** | 프로토타입에서 설정 키 미배선·순환 import·시드 계정 의존 테스트가 **전부 작성자 눈에 안 보였다**. 실행하거나 남이 봐야 드러났다 |
| **왜 에이전트** | 무거운 읽기 ✅ / 닫힌 범위 ✅ (결함 목록) / 승인 관문 없음 ✅ / 독립 시각 ✅ — **4/4** |
| **모델 fable인 이유** | 조용히 잘못되는 결함을 찾는 일이라 놓침의 대가가 크다. Phase당 1회라 총비용에 거의 영향이 없다 |
| **읽기 전용인 이유** | 수정까지 맡기면 "고쳤다"는 보고를 메인이 다시 검증해야 한다. 결함 목록이 더 값싸고 확실하다 |

### 3.3 `troubleshoot` (스킬)

| 항목 | 내용 |
|---|---|
| **문제** | 고친 기록이 대화에만 남으면 유지보수 담당자가 "이 오류 본 적 있나"를 찾을 곳이 없다 |
| **왜 에이전트가 아닌가** | **트러블이 터진 순간의 맥락(실제 오류 메시지·시도한 것)을 아는 건 메인이다.** 에이전트에 넘기면 git 기록으로 **재구성**해야 하고 그러면 부정확하다. 기준표로는 독립 시각 ❌ / 무거운 읽기 ❌ — **1/4** |
| **형태** | 서식 + 규칙. 메인이 해결 직후 직접 쓴다 |

## 4. 에이전트로 만들지 않은 것 — 그리고 이유

이 절이 없으면 나중에 "Phase 진행도 에이전트로 맡기자"는 제안이 나올 때 왜 안 되는지 찾을 곳이 없다.

| 영역 | 왜 안 맞나 | 기준표 |
|---|---|---|
| **Phase 진행 자체** | 승인 관문이 여러 개다 — 프롬프트 승인, 쟁점 결정, 리뷰, 병합 요청. 에이전트는 중간에 물어볼 수 없다 | 승인 관문 ❌ |
| **IdP·외부 연동 탐색** | 각 호출 결과가 다음 시도를 결정한다. 탐색 사이에 판단이 필요하고 그 발견이 설계를 바꾼다 | 닫힌 범위 ❌ |
| **검증기록 본문** | "무슨 일이 있었나"를 아는 건 그 자리에 있던 메인이다. 산출물만 보고 쓰면 시행착오의 **이유**가 빠진다 | 독립 시각이 오히려 해가 됨 |
| **코드 모듈 병렬 작성** | 빨라질 뿐 좋아지지 않고 토큰은 N배다. 테스트 선행 확인·스타일·통합 위험을 메인이 다시 본다 | 비용 대비 효과 ❌ |
| **실측값 → 문서 동기화** | 입력이 같으면 출력이 항상 같다. **판단이 없는 일에 에이전트를 쓰는 건 계산기 대신 사람을 고용하는 것.** 스크립트가 맞다 | 결정적 → 스크립트 |

## 5. 모델 배정 원칙과 승격 규칙

### 5.1 배정 원칙 — 빈도 × 결과물의 무게

| 빈도 | 결과물 | 모델 |
|---|---|---|
| 높음 | 메인이 검토하는 중간 산출(목록·표) | sonnet (단 놓침이 확인되면 5.2절에 따라 승격) |
| 낮음 | 판단이 필요한 산출 | opus |
| 낮음 | **미묘한 결함 탐지·최종 산출물** | fable |

자주 도는 것만 싼 모델이다. 비용의 대부분은 반복 호출에서 나오므로 거기만 아낀다.

### 5.2 승격 규칙

**결과에서 놓침이나 오탐이 확인되면 그 에이전트를 최고 모델로 올리고 되돌리지 않는다.**

- "문제" = 결과에서 **놓침이나 오탐이 확인**된 것. `doc-consistency`가 미묘한 모순을 놓치면 `CLAUDE.md` 4.2절이 경고한 *"무시되는 관문"*이 된다
- 절차: 정의 파일의 `model:`을 바꾸고 **6절에 왜 올렸는지 한 줄** 남긴다
- **처음 2~3회 실행은 메인이 결과를 독립 재검토**한다. 재검토 방법: 결과를 보기 **전에** 메인이 자기 방식(grep 등)으로 대조 기준을 만들고, 결과가 오면 비교한다. 결과를 먼저 보면 편향된다

### 5.3 정의 파일에서 모델을 비우지 않는다

`model:`을 비우면 메인 세션 모델을 상속한다. 그러면 비용 통제가 안 되고, 메인 모델을 바꾸면 에이전트 동작도 조용히 바뀐다. 전부 명시한다.

## 6. 실행 기록

| # | 일자 | 에이전트 | 모델 | 결과 | 조치 |
|---|---|---|---|---|---|
| 1 | 2026-09-16 | `self-reviewer` | fable | **첫 실행 — 합격.** Phase 0 검토에서 결함 **25건**(높음 3·보통 13·낮음 9) 보고, **오탐 0**. 명령 21개를 실제로 실행했고 컨테이너 레이아웃을 재현해 `WF_WEB_DIST` 경로 오류를 증명했다(이미지에서 SPA 404인데 헬스체크는 healthy). 메인이 못 본 것 22건. 201k 토큰, 17분 | fable 유지 (승격 사유 없음). 처리 내역은 `P0_검토서_SelfReview.md` 5절 |
| 2 | 2026-09-16 | `doc-consistency` | sonnet | **첫 실행 — 합격.** Phase 0 종료 재독에서 어긋남 **2건** 보고, 둘 다 사실로 확인됐다(**오탐 0**). ① 설계서 9.2절 CI 표가 단계를 쪼개기 전의 "`pnpm check` 한 덩어리" 상태로 남아 있고 `audit` 명령의 `--prod`도 빠져 있었다 ② FR-091의 문구가 옛 단일 명령 서술이었다. 그 밖에 "정당한 정정 이력"과 "다른 브랜치 기록"을 어긋남으로 올리지 않고 구분해 냈다. 175k 토큰, 4분 | sonnet 유지 (승격 사유 없음). 주의: 메인이 따로 대조한 축(비밀번호 정책·실측 수치·설정 키 4곳)은 에이전트 결과와 일치했다. 다만 `history/` 세션 기록이 없다는 것은 보고하지 않았다 — 문서 간 모순이 아니라 **없는 산출물**이라 이 에이전트의 범위 밖이다 |
| 3 | 2026-09-16 | `doc-consistency` | sonnet | **두 번째 실행 — 합격.** 방법론 개정 v2 재독에서 어긋남 **2건** 보고, 둘 다 사실이었다(**오탐 0**). ① 방법론개정 검토서가 **자기 문서의 줄 수를 124로 적었는데 실측 127**이었고 합계도 따라 틀렸다 ② 트러블슈팅 스킬이 인용한 문구의 절 번호가 1.3절로 적혀 있는데 실제로는 4.1절이었고 인용문도 원문과 달랐다. 이번에는 `pnpm test:cov`·`licenses:check`·`verify:docs`를 **직접 실행해 수치를 재현**하고 코드 주석의 절 번호 약 30곳을 전수 대조했다. 217k 토큰, 10분 | sonnet 유지. 두 번 연속 오탐 0이고, 자기 참조 집계 오류처럼 사람이 놓치기 쉬운 것을 잡았다. `CLAUDE.md` 4.2절의 "처음 2~3회는 메인이 독립 재검토" 조건을 이번으로 2회 채웠다 |
| 4 | 2026-09-17 | `doc-consistency` | sonnet | **세 번째 실행 — 놓침 확인.** 작업 계획 프롬프트 폐지 재독에서 **어긋남 0건**을 보고했다. 메인이 직접 대조하니 **1건이 있었다**: `.claude/agents/doc-consistency.md`의 예외 규칙에 "구버전 프롬프트의 내용은 보고하지 않는다"가 남아 있었다. 프롬프트 버전 페어링 규칙은 v2 개정에서 폐지됐고 남은 요청 기록은 전부 유효하므로, **실재하지 않는 범주를 예외로 두어 유효한 문서를 검사 밖으로 밀어낼 수 있는 상태**였다. 이 파일은 메인이 검사 대상으로 명시했고 에이전트는 그 파일이 "현재 구조를 정확히 반영한다"고 단정했다. 88k 토큰, 1분 | **fable로 승격 (5.2절, 되돌리지 않는다).** 0건 보고는 가장 값싼 출력이라 확인 비용이 가장 큰데, 이번 실행은 토큰·시간이 앞선 두 번의 절반 이하였다. 예외 규칙도 함께 고쳤다 |
| 5 | 2026-09-22 | `self-reviewer` | fable | **Phase 3.** 결함 **20건**(높음 1·보통 6·낮음 13), 오탐 0. 높음은 "첨부 쓰기가 원자적이지 않다" — 쓰는 중에 죽으면 잘린 파일이 해시 자리에 굳고 다운로드가 오류 없이 잘린 내용을 준다. 명령 6개를 실제로 실행했고, `pnpm test:e2e`를 **적힌 그대로** 쳐서 실패하는 것을 증명했다 | fable 유지. 처리 내역은 `P3_검토서_SelfReview.md` |
| 6 | 2026-09-22 | `doc-consistency` | fable | **Phase 3.** 어긋남 **30건**, 오탐 0. 이번 변경이 만든 것과 **이전 Phase부터 방치된 것**을 구분해 냈다. 가장 값있던 것은 리눅스빌드 가이드의 **볼륨 목록** — 공유 서버에서 "지워도 되는 것"을 정하는 문장에 `deploy_attachments`가 빠져 있었다 | fable 유지 |
| 7 | 2026-09-22 | `self-reviewer` | fable | **Phase 4.** 결함 **17건**(높음 2·보통 8·낮음 7), 오탐 0. 높음 둘: 페이지 본문 멘션에 호출부가 없다(조용히 아무 일도 안 함), 비밀번호 정책을 **낮추는** 방향이 안 먹는다. 그리고 **검증기록에 내가 적은 커버리지 수치가 틀린 것**을 잡았다(A등급 미달을 패키지 합계가 가리고 있었다) | fable 유지. 2건을 제외한 전부를 이 Phase에서 고쳤다 |
| 8 | 2026-09-22 | `doc-consistency` | fable | **Phase 4.** 어긋남 **36건**, 오탐 0. 범위 문서가 Phase 4에 배정한 기능 넷(템플릿·내보내기, 세션 강제 종료, 권한 세분화, 알림 범위)이 **설계서 어디에도 없다**는 것을 짚었다 — 그대로 두면 Phase가 닫힐 때 사라진다 | fable 유지 |

| 9 | 2026-09-22 | `self-reviewer` | fable | **Phase 5.** 결함 **17건**, 오탐 0. 가장 값있던 것: **복원이 절반만 되고 되돌릴 수도 없다** — 절차서는 api를 먼저 멈추라 하는데 복원이 `exec api`로 첨부를 푼다. 리허설이 그것을 통과한 이유는 **내가 절차서대로 리허설하지 않았기 때문**이었다 | fable 유지 |
| 10 | 2026-09-22 | `doc-consistency` | fable | **Phase 5.** 어긋남 **52건**, 오탐 0(8건은 과거 서술로 정당 판정). 가장 값있던 것: 반입 가이드의 사후 검증이 `docker kill`로 자동 기동을 확인하라고 하는데, **같은 Phase의 T-025가 그 방법은 되살아나지 않는다고 밝혔다** — 반입 당일 작업자가 정상 시스템을 고장으로 판정할 자리였다 | fable 유지 |
| 11 | 2026-09-22 | `/security-review` | (내장) | **Phase 5.** High **1건**. 복원이 표 이름을 `BACKUP.json`의 키에서 읽어 SQL에 넣는데 그 파일이 **체크섬 밖**이었다 — 백업 공유 폴더에 쓸 수 있는 사람이 DB 계정 없이 관리자 권한 SQL을 실행할 수 있었다 (T-029) | 세 겹으로 막고 실제 조작 백업으로 재현 확인 |
| 12 | 2026-09-22 | `/code-review` | (내장) | **Phase 5.** 16건 + 확인 필요 4건. 가장 값있던 것: **`scripts/**`가 타입 검사를 한 번도 받지 않았다** — 반입·백업·복원 도구 전부다. 그 지적을 고치자마자 **그 검사가 다른 지적의 수정에서 생긴 오류를 바로 잡았다** (T-030) | 16건 반영 |
| 13 | 2026-09-22 | `self-reviewer` | fable | **Phase 6.** 결함 **34건**, 오탐 0. 가장 값있던 것: TipTap 링크가 늘 붙이는 `title: null` 때문에 **링크가 하나라도 있는 문서는 협업 저장이 통째로 거부**되고, 화면에는 아무 표시도 안 났다 | 19건 반영. 나머지는 `P6_검토서_SelfReview.md` §3 |
| 14 | 2026-09-23 | `doc-consistency` | fable | **Phase 6 (한 Phase 늦게).** 어긋남 **31건**, 오탐 0. 정당한 과거 서술 13건을 구분해 냈다. 가장 값있던 것: 장애대응 문서가 같은 문서 안에서 **"알림은 메일로 가지 않는다"와 "메일이 안 올 때"를 함께** 말하고 있었다 | 31건 전부 반영 (`P7_검토서_Review.md`) |
| 15 | 2026-09-23 | 보안 검토 (`general-purpose`에 `/security-review` 지침) | opus | **Phase 6 (한 Phase 늦게).** HIGH **1건** + 확신도 8 미만 4건, 오탐 0. "세션을 끊어 침해를 봉쇄한다"는 대응이 **Phase 6이 만든 채널에서는 동작하지 않는다**를 짚었다. 확인했고 문제가 없던 것을 항목별로 남겨 준 것이 값있었다 — 다음에 같은 자리를 다시 볼 필요가 없다 | HIGH + 참고 3건 반영 |
| 16 | 2026-09-23 | 코드 리뷰 (`general-purpose`에 `/code-review` 지침) | opus | **Phase 6 (한 Phase 늦게).** 높음 6 · 중간 7 · 낮음 8, 오탐 0. 가장 값있던 것 둘: **창을 닫으면 프로세스가 죽는다**(그리고 권한 있는 사람이 원할 때 일으킬 수 있다), **비교가 링크 주소 변경을 "같다"고 답한다.** 후자는 E2E 단언이 **절대 실패할 수 없는 모양**이라 통과하고 있었다 | 18건 반영, 3건 사유와 함께 남김 |
| 17 | 2026-09-23 | `self-reviewer` | fable | **Phase 7 (병합 전).** 결함 **13건**(높음 3·보통 5·낮음 5), 오탐 0. 높음 셋이 전부 **메인이 이번에 고치면서 새로 만든 것**이었다 — `joining` 표가 영영 안 내려가는 경로, 검증 실패 시 사람이 남아 있는 방을 지우는 것, `flush`가 "방이 있었다"를 "남겼다"로 답하는 것. `ws`의 `handleUpgrade` 원문까지 읽어 콜백이 안 불리는 조건을 짚었다 | 13건 전부 반영 |
| 18 | 2026-09-23 | 보안 검토 (`general-purpose`) | opus | **Phase 7 (병합 전).** 5건(MEDIUM 4·LOW 1), 오탐 0. **새로 넣은 방어가 닿지 않는 구멍**을 찾았다 — 로그아웃이 버스를 울리지 않고, `close()`가 30초 동안 메시지를 더 받고, `flush`에 감사 기록이 없고, CR/LF 금지가 주 로그인 경로(OIDC)를 비켜 간다 | 5건 전부 반영. 참고 1(스킴 검사)은 **넣었다가 되돌렸다** — 앞단 구성에 따라 전면 장애가 된다 |
| 19 | 2026-09-23 | `doc-consistency` | fable | **Phase 7 (병합 전).** 어긋남 **22건**, 오탐 0. 핵심 하나로 여러 문서를 갈랐다 — "로그아웃하면 끊긴다"가 코드와 맞지 않는다는 것을 짚고, 그 한 사실이 인수 기준·학습가이드·`CLAUDE.md` 셋을 동시에 틀리게 만든다는 것을 보였다 | 22건 전부 반영. 그 지적이 보안 검토 F1과 같은 자리였다 |
| 20 | 2026-09-24 | `self-reviewer` | fable | **Phase 8 (병합 전, 첫 판).** 7건(높음 1·보통 2·낮음 4), 오탐 0. 높음은 **보류 조각 위조** — 남의 ID로 앞선 시계의 조각을 보내 두면 주인의 변경에 묻어 들어온다. 재현 스크립트까지 만들어 "A 님이 불렀다"가 나오는 것을 보였다 | 전부 반영(1건은 적어 둠) |
| 21 | 2026-09-24 | 코드 리뷰 (`/code-review high`) | opus | **Phase 8 (병합 전, 첫 판).** 9건, 오탐 0(효율 1건은 전제가 틀려 두지 않음). **남이 글자를 지워 만든 멘션**이 원래 쓴 사람 이름으로 나가는 것을 짚었다 | 8건 반영 |
| 22 | 2026-09-24 | 보안 검토 (`general-purpose`) | opus | **Phase 8 (병합 전, 첫 판).** MEDIUM 1건(8/10) — 코드 리뷰 1과 같은 자리를 다른 길(끼워 넣기 포함)로 찾았다 | 반영 |
| 23 | 2026-09-24 | `doc-consistency` | fable | **Phase 8 (병합 전, 첫 판).** 7건 + 과거 기록 판단 9건, 오탐 0 | 7건 반영 |
| 24 | 2026-09-24 | `self-reviewer` | fable | **Phase 8 (병합 전, 둘째 판).** 9건(높음 3·보통 4·낮음 2), 오탐 0. **둘째 판의 사후 추론을 세 가지로 다시 뚫었다**(보류 삭제 집합·`rightOrigin` 위조·흔적 앞 끼우기) — 실제 y-prosemirror 1.3.7로 정상 사용의 오판 셋도 재현했다. 이 결과로 **설계를 바꿨다**: 구조를 믿지 않고 인증된 연결만 믿는다 | 귀속 **방식**을 바꿔(셋째 판) 전부 닫음 |
| 25 | 2026-09-24 | `doc-consistency` | fable | **Phase 8 (병합 전, 둘째 판).** 22건, 오탐 0. 대부분 "친 사람" 같은 첫 판 표현의 잔재였다 | 셋째 판 문서를 다시 쓰며 반영 |
| 26 | 2026-09-24 | 코드 리뷰 (`/code-review high`) | opus | **Phase 8 (병합 전, 셋째 판).** 7건, 오탐 0. **셋째 판의 사칭 하나를 찾았다** — 공격자가 피해자 커서 뒤에 멘션이 될 글을 써 두면 피해자의 공백 하나가 피해자 이름으로 멘션을 만든다. 같은 Y.Doc 재접속, 동시 편집의 거짓 경고도 짚었다 | 정확성 4건 반영(넷째 판), 효율 3건 중 1건 반영 |
| 27 | 2026-09-24 | `doc-consistency` | fable | **Phase 8 (병합 전, 셋째 판).** 9건, 오탐 0. 판이 바뀔 때마다 남는 옛 표현을 코드 주석까지 짚었다 | 9건 반영 |
| 28 | 2026-09-24 | `self-reviewer` | fable | **Phase 8 (병합 전, 셋째 판).** 7건(높음 2·보통 3·낮음 2), 오탐 0. 작업 트리가 넘어가자 **셋째 판을 worktree·별도 DB에 따로 세워** 돌렸다. 심기 공격을 경합·커서 성질로 재현하고, **이상한 노드 하나로 방의 중계와 저장이 멈추는 것**을 찾았다 — 부가 기능의 예외가 편집을 멈추는 자리 | 넷째 판과 FR-909로 전부 반영(1건은 남는 것으로 적음) |
| 29 | 2026-09-24 | 코드 리뷰 (`/code-review high`) | opus | **Phase 8 (병합 전, 넷째 판).** 8건, 오탐 0. 옮김 기억의 경합·자람 예외 우회·관찰자 예외의 근본 원인, 그리고 **검증기록에 경위를 쓴 규칙 위반**을 짚었다 | 7건 반영, 1건 보류 22 |
| 30 | 2026-09-24 | `doc-consistency` | fable | **Phase 8 (병합 전, 넷째 판).** 13건, 오탐 0. 규칙이 한 번 바뀐 뒤 **한 구절이 다섯 곳에 복제된 채 남은 것**을 한 묶음으로 짚었다 | 13건 반영 |
| 31 | 2026-09-24 | `self-reviewer` | fable | **Phase 8 (병합 전, 넷째 판).** 8건(보통 3·낮음~보통 1·낮음 4), **높음 0**, 오탐 0. 검토 중 코드가 바뀌자 넷째 판을 worktree·별도 DB에 따로 세웠다. 한글 IME·자동 완성·undo까지 실제 y-prosemirror 모양으로 통과시켜 정상 사용을 확인하고, **정상 사용에서 이름이 비는 넷**과 동료 화면이 깨지는 것을 찾았다 | 5건 고침, 1건 보류 23, 2건 §7.1과 같음 |
| 32 | 2026-09-24 | `doc-consistency` | fable | **Phase 8 (병합 전, 넷째 묶음 반영분 `0eeced1..38add7f`).** 9건(규칙과 반대말 1 · 낡은 서술 2 · 낡은 모양 2 · 표현 4), 오탐 0. **T-035 이전 동작을 현재형으로 적은 설계서 문단**을 FR-909와 대조해 반대말로 짚었다 — 기계 검사로는 잡히지 않는 종류 | 9건 반영 |
| 33 | 2026-09-24 | `self-reviewer` | fable | **Phase 8 (병합 전, 같은 범위).** 5건(보통 1 · 낮음 4), **높음 0**, 오탐 0. 실제 게이트웨이로 스크래치 시험 6건을 세워 **검토서 §7.4의 내 판단("먼저 알아본 사람이 임자")이 방어가 아님**을 보였다 — 화면이 커서 정보로 클라이언트 ID를 첫 편집 전에 알린다. 서버 코드만 읽어서는 나오지 않는 사실이다. 이 차이 밖의 P6 결함(바뀌지 않은 문서의 매초 유휴 판정)도 재현했다 | 3건 고침(FR-910 포함), 1건 보류 24, 1건 사유 기록 |
| 34 | 2026-09-24 | `self-reviewer` | fable | **Phase 9 (병합 전, 첫 판).** 3건(보통 2·낮음 1), 오탐 0. 실제 편집기로 만든 정상 편집 40여 종을 관문에 넣어 **오거절 0**을 먼저 확인하고, 관문이 깊이 한도를 보지 않아 저장이 조용히 멈추는 것을 **통합 시험의 주석이 시인하고 있다**는 것으로 짚었다 | 전부 반영 |
| 35 | 2026-09-24 | 코드 리뷰 (`/code-review high`) | opus | **Phase 9 (병합 전, 첫 판).** 10건, 오탐 0. 정상 동시 편집을 끊는 "지워진 부모"와 한 사람이 서버를 몇 분 멈추는 제곱 비용을 실제 관문으로 재현했다 | 8건 반영, 2건(효율·재사용) 까닭을 적고 두지 않음 |
| 36 | 2026-09-24 | 보안 검토 (`general-purpose`) | opus | **Phase 9 (병합 전, 첫 판).** MEDIUM 1건(8/10) — 설계서가 "해가 없다"고 한 표 칸 `align`이 `style`로 들어가 화면 전체를 덮는다. 문턱 아래 후보(되풀이된 덩어리)도 결함이었다 | 반영 |
| 37 | 2026-09-24 | `doc-consistency` | fable | **Phase 9 (병합 전, 첫 판).** 17건, 오탐 0 | 17건 반영 |
| 38 | 2026-09-24 | `self-reviewer` | fable | **Phase 9 (병합 전, 첫 묶음 반영분).** 8건(보통 2·낮음 6), **높음 0**, 오탐 0. 하니스를 받은 바이트까지 넘기게 고쳐 오거절 0, 완결 규칙을 Yjs와 차분 퍼즈 2,160건으로 대조해 불일치 0. **캐럿이 Phase 6부터 투명**이라는 것을 소스 대조로 찾았다 | 반영(1건은 병합 전에 닫힘) |
| 39 | 2026-09-24 | 코드 리뷰 (`/code-review high`) | opus | **Phase 9 (병합 전, 첫 묶음 반영분).** 9건, 오탐 0. 다시 붙을 때 사람 표시가 비는 것, 풀림을 커밋 전에 보내는 것, 상한이 연결마다라 새는 것 | 8건 반영, 1건 까닭을 적고 두지 않음 |
| 40 | 2026-09-24 | 보안 검토 (`general-purpose`) | opus | **Phase 9 (병합 전, 첫 묶음 반영분).** MEDIUM 1건(9/10) — **첫 묶음이 막았다고 적은 커서 그리기 깨짐이 그대로였고, 헤드리스 브라우저로 동료들의 편집이 지워지는 것까지 재현했다.** 문턱 아래 후보 둘(아는 조각의 이웃·일부만 아는 조각의 자리)도 재현해 보니 실제였다 | 전부 반영 |
| 41 | 2026-09-24 | `doc-consistency` | fable | **Phase 9 (병합 전, 첫 묶음 반영분).** 14건, 오탐 0. 첫 묶음의 17건 반영도 파일에서 다시 확인했고, 규칙서와 설계서가 같은 사실을 반대로 적은 곳(보류 25)을 짚었다 | 14건 반영 |
| 42 | 2026-09-24 | `self-reviewer` | fable | **Phase 9 (병합 전, 두 번째 묶음 반영분).** 6건(보통 1·낮음 5), 오탐 0. 하니스를 **화면이 쓰는 `@tiptap/y-tiptap` 3.0.9**로 다시 돌려 정상 편집 23건 오거절 0·커서 170위치 누락 0·정본 왕복 22종 오판 0. **사람 표시 ID 풀기의 경합을 실제 게이트웨이로 재현했다** — 피해자가 감사로그에 조작자로 남는다 | 반영(검증기록 절 번호는 검증기록을 쓸 때) |
| 43 | 2026-09-24 | 코드 리뷰 (`/code-review high`) | opus | **Phase 9 (병합 전, 두 번째 묶음 반영분).** 7건, 오탐 0. 일부만 아는 **지운** 조각이 자리 검사를 건너뛰는 것, 사람 표시 ID 풀기의 경합(자체 점검과 같은 결함을 따로 찾았다), 저장이 던질 때 알림이 없는 것 | 7건 반영(편집기 정리는 `StrictMode` 때문에 제안과 다른 모양으로) |
| 44 | 2026-09-24 | 보안 검토 (`general-purpose`) | opus | **Phase 9 (병합 전, 두 번째 묶음 반영분).** 문턱을 넘는 것 없음. 조작한 커서 26가지를 동료 쪽 경로에 넣어 던짐 0, 무작위 차분 퍼즈 7,200건 놓침 0·어긋남 0. 경합(피해자 자신의 닫힘 뒤 차지)은 다루지 않았다 — 코드 리뷰와 자체 점검이 찾았다 | — |
| 45 | 2026-09-24 | `doc-consistency` | fable | **Phase 9 (병합 전, 두 번째 묶음 반영분).** 12건, 오탐 0 | 12건 반영(4번은 `87657d9`에서) |
| 46 | 2026-09-25 | `self-reviewer` | fable | **Phase 9 (병합 전, 세 번째 묶음 반영분).** 4건(보통 1·낮음 3), 오탐 0. 지운 조각 자리 검사를 화면 모양의 정상 편집으로 돌려 오거절 0을 먼저 확인하고, 저장 실패 알림이 **트랜잭션 앞의 DB 읽기**를 덮지 않는 것을 찾았다 | 전부 반영 |
| 47 | 2026-09-25 | `doc-consistency` | fable | **Phase 9 (병합 전, 세 번째 묶음 반영분).** 8건, 오탐 0. 이번 커밋이 스스로 깨뜨린 근거 문장(FR-1004)을 짚었다 | 7건 반영, 1건(인수 기준 열거) 두지 않음 |
| 48 | 2026-09-25 | `self-reviewer` | fable | **Phase 10 (병합 전, 첫 판).** 10건(높음 1·보통 2·낮음 7), 오탐 0. 높음은 **토큰 수가 감사로그에 닿지 않는다** — 실제 `sanitizeDetail`에 그 모양을 넣어 두 키가 버려지는 것을 보였다(T-043). 실제 HTTP 소켓으로 끊김 감지가 **출구를 만든 뒤의 끊김만** 잡는다는 것도 쟀다 | 전부 반영(1건은 판단 유지 + 문서화) — `P10_검토서_Review.md` |
| 49 | 2026-09-25 | 코드 리뷰 (`/code-review high`) | opus | **Phase 10 (병합 전, 첫 판).** 15건 + 상한 밖 9건, 오탐 0. **시험이 결함을 기대값으로 고정한 것**(흐름 도중 LLM 삭제로 저장 실패를 만들던 시험)과 흐름이 화면 수명에 묶이지 않은 것을 짚었다. 닫는 태그만 오는 Qwen3 모델, undici의 300초, 세로로 합친 표 칸까지 실제로 재현했다 | 23건 반영, 1건(대화 다시 읽기) 까닭을 적고 두지 않음 |
| 50 | 2026-09-25 | 보안 검토 (`general-purpose`) | opus | **Phase 10 (병합 전, 첫 판).** LOW 3건(8~9/10) + 문턱 아래 6건, 오탐 0. **문턱 아래 후보 하나가 이 Phase 밖까지 닿았다** — 주소의 `%2F`로 다른 API를 부르게 하는 경로 조작이 기존 화면의 PUT에도 있었다. AAD가 주소를 묶지 않는 것은 실제 서비스로 재현했다 | 전부 반영(빈도 제한은 보류 30). 경로 조작은 `api()` 한 곳에서 막았다 |
| 51 | 2026-09-25 | `doc-consistency` | fable | **Phase 10 (종료 루틴, 반영분 `cf00fa5..4af97a9`).** 12건 + 이번 변경 밖 2건, 오탐 0. 정당한 과거 서술 11건을 구분해 냈다. 새로 둔 FR-1122를 **이번 커밋의 주석 셋이 옛 번호(FR-1115)로 가리키는 것**을 짚었다 | 13건 반영, 1건(P7 설계서) 두었다 |
| 52 | 2026-09-25 | `self-reviewer` | fable | **Phase 10 (종료 루틴, 같은 범위).** 8건(보통 1·낮음 7), 오탐 0. 반영분이 규칙서에 세운 "PostgreSQL의 코드·문장만 싣는다"를 **실제 drizzle + PostgreSQL 오류로 뚫었다** — 데이터 예외는 문장에 값을 싣는다. 시험이 합성 오류만 보던 것을 짚었다. 자기 빌드(:3200)로 E2E 28건을 다시 돌리고 끝낸 뒤 서버를 내렸다 | 6건 고침, 2건 판단을 적음 — `P10_검토서_Review.md` §5 |
| 53 | 2026-09-26 | `self-reviewer` | fable | **Phase 11 (병합 전, 첫 판).** 11건(높음 1·보통 3·낮음 7), 오탐 0. 높음은 **가이드의 명령과 컨테이너 확인에서 쓴 명령이 다르다**(`up -d` ↔ `restart`) — 파일만 둔 뒤의 `up -d`는 아무것도 하지 않는다(T-049). `:3100` 로그 786줄을 읽고 없는 API 주소가 `{*any}`로 남는 것을 재현했다(T-048). `verify:docs`가 빌드 산출물의 목록을 읽는다는 것도 짚었다 | 10건 반영, 1건(인덱스) 실측으로 두지 않음 — `P11_검토서_Review.md` |
| 54 | 2026-09-26 | 코드 리뷰 (`/code-review high`) | opus | **Phase 11 (병합 전, 첫 판).** 10건, 오탐 0. 자체 점검과 같은 `up -d`를 따로 찾았고, **묶음에 `ca/`가 없으면 도커가 root 소유로 만든다**를 더했다(실측으로 확인). 역할 변경이 잠그지 않은 스냅숏으로 위임을 덮는 경합, 요청 문맥의 `userId`를 대상 필드가 덮는 것, 사내 IdP 실패에 event가 없는 것 | 10건 반영(둘은 일부 — 인덱스·대상의 상태는 까닭을 적음) |
| 55 | 2026-09-26 | 보안 검토 (`general-purpose`) | opus | **Phase 11 (병합 전, 첫 판).** MEDIUM 1 · LOW 1(8·9/10) + 문턱 아래 4, 오탐 0. **다른 둘이 못 본 것을 찾았다** — 위임이 관리자 사이에 차이를 만들면서 위임 없는 관리자가 위임받은 관리자의 비밀번호를 초기화해 계정을 넘겨받는 길이 열렸다. 대문자 경로가 접근 로그를 비켜 가는 것을 `:3100`에서 재현했다 | 둘 다 반영(관리의 우열 — `canManageUser`가 가진 것을 본다), 문턱 아래는 1건 반영·1건 막힘·2건 까닭을 적음 |
| 56 | 2026-09-26 | `doc-consistency` | fable | **Phase 11 (종료 루틴, 반영분 `1032c78..1267933`).** 14건 + 이번 변경 밖 4건, 오탐 0. 정당한 과거 서술 18건을 구분했다. **장애대응 7.24가 "사내 CA의 자리가 아직 없다"**고 말하던 것 — Phase 11 본작업이 그 자리를 만들고 남긴 반대말을 짚었다. "위임받은 관리자는 시스템 관리자만 관리한다"가 규칙(같은 위임을 가진 관리자도)보다 좁게 세 곳에 퍼진 것도 | 13건 반영, 1건(scripts 목록) 두었다 — `P11_검토서_Review.md` §5 |
| 57 | 2026-09-26 | `self-reviewer` | fable | **Phase 11 (종료 루틴, 같은 범위).** 9건(보통 1·낮음 8), 오탐 0. jose로 서명·aud·exp 오류를 만들어 **id_token 검증 실패가 502 "잠시 뒤 다시"로 간다**를 보였고, OIDC 콜백 실패가 감사에 없는 것(Phase 1부터)과 마지막 root 강등의 경합(Phase 1부터)을 짚었다. `bundleFiles`의 링크를 프로브로 죽였다 | 9건 반영 — `P11_검토서_Review.md` §5 |

### 17~19에서 배운 것 — **고친 직후가 가장 위험하다**

Phase 7은 "남이 만든 결함을 닫는" Phase였는데, 병합 전 검토에서 나온 **높음 셋이 전부
이번에 고치면서 새로 생긴 것**이었다. 고치는 손이 가장 바쁜 자리가 가장 덜 읽힌 자리다.
`CLAUDE.md` 1.1절 8-1의 "병합 전 검토"는 형식이 아니다.

### 14~16에서 배운 것 — **병합한 뒤에 돌리면 안 된다**

`/code-review`와 `/security-review`는 "이 브랜치의 변경분"을 본다. Phase 6은 이미 `main`에
병합돼 그 차이가 **비어 있었다.** 그대로 불렀으면 아무것도 보지 않고 "문제 없음"이 나왔을
것이다 — 가장 나쁜 결과다. 같은 지침을 병합 범위(`6f143e8..97ecaa3`)에 적용해 내장 스킬 대신
`general-purpose` 에이전트로 돌렸다.

**그리고 셋이 서로 다른 것을 봤다.** 보안 검토의 HIGH 1건과 코드 리뷰의 "중간 9번"은 같은
자리인데 심각도 판정이 갈렸고, 문서 검사는 "만들어 놓고 문서에 안 적은 것"을 냈다.
하나로 대신할 수 없다.

**네 번 연속 오탐 0이다.** 두 에이전트 모두 "메인이 못 본 것"을 매번 냈고, 특히
`self-reviewer`는 **메인이 쓴 검증기록의 수치가 틀린 것**을 두 Phase 연속으로 잡았다
(P3의 측정 대상 누락, P4의 수치 오기). 자기 작업을 자기가 검토하는 것의 한계가
반복해서 드러난 자리다.
