# P4_검토서_SelfReview — Phase 4 자체 점검 (중간)

- 일자: 2026-09-22 / 브랜치 `impl-phase4`
- 돌린 것: `self-reviewer`(17건), `doc-consistency`(36건)
- **돌리지 않은 것**: `/code-review`, `/security-review`. `CLAUDE.md` 1.1절 5단계는 Phase 4에 보안 점검을 **필수**로 둔다 — 사용량 상한에 가까워 멈췄다. **Phase 4를 닫기 전에 반드시 돌린다.**
- Phase가 끝나지 않았으므로 이 문서도 중간 기록이다.

---

## 1. 고친 것

| # | 지적 | 처리 |
|---|---|---|
| 자체 6 | **검증기록의 `policy.ts` 커버리지가 실측과 다르다** — 100/100이라 적었는데 브랜치 89.28%였고, A등급 기준(90%) 미달인데 패키지 합계가 가려 관문은 초록이었다 | 비어 있던 분기(DB의 유효한 확장자 목록을 얹는 경로)에 테스트를 넣어 **100/100/100/100**으로 만들고, 검증기록 수치를 실측으로 갈았다 |
| 문서 12건 | 단일 출처가 틀린 것들 — 아키텍처 3.1절의 `notifications` 컬럼(`type`·`payload`는 없다), 코드 트리의 없는 `system/`, 빠진 `policy.ts`·`trash-purge`, `CLAUDE.md` 4.1절 스크립트·6절 감사 대상, 보류 7 이정표, **보류 10 종료 표기**, 운영가이드의 "복원 화면은 Phase 4"·확장자 목록 안내 | 제자리 개정 |
| 문서 23 | 검증기록이 **같은 문서 안에서 반대말**을 했다(가이드 갱신을 5절은 "미이행", 6절은 "했다") | 5절을 사실에 맞춰 지웠다 |
| 자체 1 (높음) | **페이지 본문의 `@아이디`가 알림을 만들지 않았다.** 설계서 FR-500은 "댓글·페이지 본문 둘 다"인데 호출부가 댓글에만 있었다 — 오류 없이 조용히 아무 일도 안 했다 | 페이지 생성·저장 둘 다에 붙이고 테스트 2건을 넣었다. **설계서가 맞았고 코드가 빠져 있었다** |

**커버리지 수치가 틀린 것이 가장 뼈아프다.** T-020·T-024가 "측정 대상이 맞는가"를 두 번
가르쳤는데, 이번에는 대상은 맞았고 **내가 옮겨 적은 숫자가 틀렸다.** 검증기록은 서술이
아니라 수치라고 규칙(4절)에 적어 두고, 그 수치를 손으로 옮기다 틀렸다. 다음 Phase에서는
**커버리지 표를 손으로 쓰지 않고 실측 출력에서 만들어 넣는다.**

---

## 2. 남긴 것 — Phase 4를 닫기 전에 해야 한다

### 기능 결함

| # | 무엇 | 왜 지금 안 했나 |
|---|---|---|
| 자체 2 (높음) | **비밀번호 문자 종류를 낮추는 방향은 안 먹는다.** zod의 `passwordSchema`가 파싱 시점에 상수 2종을 먼저 강제해, 정책값을 1로 내려도 서비스에 닿기 전에 400이다. 검증기록의 "비밀번호 강도가 정책값을 쓴다"는 **올리는 방향만** 참이다 | 검증기록을 정정했다. 고치려면 zod 스키마에서 강도 판정을 빼고 서비스로 모아야 한다 |
| 자체 4 | **개별로 지운 첨부·댓글은 영영 물리 삭제되지 않는다.** `trash:purge`가 "만료된 페이지·스페이스와 그 자식"만 본다. 쟁점 5는 "넷 전부"였다 | |
| 자체 3 | 정책값 캐시가 **커밋되지 않은 값**으로 채워질 수 있다. `update()`가 같은 트랜잭션에서 읽어 캐시에 넣고, 그 뒤 감사 기록이 실패해 롤백되면 DB와 메모리가 어긋난다 | |
| 자체 5 | 업로드 천장이 **쓰기에서만** 검사된다. DB에 50이 저장된 뒤 환경변수를 20으로 내려 재기동하면 FR-528이 막으려던 상태가 다시 열린다 | |
| 자체 9 | FR-506이 **페이지 삭제에만** 구현됐다. 댓글·스페이스가 지워진 경우는 링크가 정상으로 보인다 | |
| 자체 10 | `PATCH /api/settings/policy` 본문과 `GET /api/labels/:name/pages`의 `name`이 **zod를 지나지 않는다** (`CLAUDE.md` 7절 위반) | |
| 자체 12 | 정책값 변경이 읽기-병합-쓰기를 잠금 없이 한다. 두 관리자가 동시에 바꾸면 한쪽이 조용히 사라진다 | |

### 과정 결함

| # | 무엇 |
|---|---|
| 자체 8 | **A등급 둘의 "테스트 먼저"가 커밋 이력으로 확인되지 않는다.** 실제로는 테스트를 먼저 썼지만 같은 커밋에 넣어, `CLAUDE.md` 1.5절 3단계가 요구하는 증거가 남지 않았다. 다음부터는 **테스트만 먼저 커밋한다** |
| 자체 7 | "잠금 임계가 정책값을 쓴다"를 단언하는 테스트가 실제로는 `get()` 값만 본다. 로그인 실패를 일으키지 않는다 |
| 자체 15 | NFR-42(휴지통 p95)를 재지 않았는데 "확인하지 못한 것"에도 없었다 |
| 자체 17 | `trash:purge`와 E2E 정리가 고아 라벨을 남긴다 — 서비스의 "고아 라벨은 지운다"와 어긋난다 |
| 문서 4·7·8 | `scope-definition`이 Phase 4에 배정한 **템플릿·HTML 내보내기**와 **세션 강제 종료**가 설계서에 없다. 권한 세분화는 "하지 않기로" 했는데 범위 문서가 그대로다 |
| 문서 36 | 이전 Phase가 "Phase 4에서"로 넘긴 항목 10여 건이 설계서에 채택도 재연기도 안 됐다. **설계서에 없으면 Phase가 닫힐 때 조용히 사라진다** |

---

## 3. 검토가 확인해 준 것 (다시 논하지 않는다)

- 알림 권한이 `spaceAccess().canRead` 한 곳을 쓴다. 개인·중지·삭제 스페이스 전부 일치.
- 휴지통 목록 SQL이 `canWrite`와 같은 결과를 낸다.
- 라벨 찾기 SQL이 검색과 **글자 그대로 같은** 권한 조건.
- **rate-limit 환불에 남의 몫을 지우는 경로가 없다.** 같은 ms·배열 교체·두 번 호출·만료 뒤 전부 확인됐다.
- `trash:purge`의 삭제 순서가 FK를 어기지 않는다 (자식이 부모보다 먼저 지워지는 것이 보장된다).
- skip·todo 0건.
