# P11 검토서 — 운영 로그·LLM 관리 위임·사내 LLM 반입 설정의 병합 전 검토와 그 처리

- 작성일: 2026-09-26 / 작성 LLM: Claude Opus 5.5
- 대상: `main...impl-phase11` — 병합 전에 돌렸다 (CLAUDE.md 13절). HEAD `1032c78`(코드·문서·검증기록까지)
- 반영: Red `4c18ad3` → Green `e2ffff6`(묶음·event·요청 번호·감사 조회·전체 잡기 라우트) · Red `015ddce` → Green은 §3 끝의 커밋(관리의 우열·대소문자·
  잠금·IdP·로그 필드·화면)
- 종료 루틴(1.1절 8-1)의 `doc-consistency`·`self-reviewer`는 반영분에 돌린다 — §5

## 1. 무엇을 돌렸나

| 검토 | 결과 | 오탐 |
|---|---|---|
| `self-reviewer` | 11건 (높음 1 · 보통 3 · 낮음 7). `:3100` 로그 786줄을 읽고 404를 재현했다 | 0 |
| 코드 리뷰 (`/code-review high`, PR #10) | 10건 | 0 |
| 보안 검토 (`/security-review` 지침, `general-purpose`) | MEDIUM 1 · LOW 1 (확신 8·9) + 문턱 아래 4 + 보안 밖 1 | 0 |

`/security-review` 스킬은 이 계정에서 옛 트리를 보므로(T-040·T-042) Phase 10처럼 같은 지침을 `general-purpose` 에이전트에 주어 이 계정의
사본을 보게 했다. 셋이 겹친 것: 사내 CA를 두고 다시 띄우는 명령(코드 리뷰 1 · 자체 점검 1), 묶음의 `ca/`(코드 리뷰 3 · 자체 점검 4),
역할 변경의 경합(코드 리뷰 4 · 자체 점검 6 · 보안 후보 1), 시드(코드 리뷰 2 · 자체 점검 5 · 보안 밖), `request_id` 인덱스(코드 리뷰 10 ·
자체 점검 8). **가장 큰 것은 보안 검토만 찾았다**(①).

## 2. 가장 값있던 것

### ① 위임이 관리자 사이에 차이를 만들면서, 계정을 넘겨받는 길이 새로 열렸다 (보안 검토 1)

Phase 11 전에는 관리자끼리 권한이 같아 동료의 계정을 넘겨받아도 얻는 것이 없었다. 위임이 그 차이를 만들었다 — 위임 없는 관리자가 위임받은
관리자의 **비밀번호를 초기화하면** 임시 비밀번호로 그 계정에 들어가 LLM 연결 관리를 얻는다. 정상 LLM을 지우고 같은 이름으로 자기가 연 주소를
등록하면 직원들의 질문이 그 주소로 간다. 역할을 member로 내렸다 올리면 root가 준 위임을 거둔다(보안 후보 3 — 같은 뿌리).
설계의 "주고 거두는 권한이 root에게 남아야 메인이다"(A.1-3)를 옆으로 돌아가는 길이었다.

→ `canManageUser`가 역할과 함께 **가진 것**을 본다: root가 아니면 대상의 위임을 자기도 가져야 관리한다(승인·초기화·역할·잠금 해제·세션 종료가
모두 이 판정을 지난다). 화면도 같은 함수로 그 행의 조치를 막는다. 설계서 A.1-14, FR-1207.

### ② 문서의 명령이 확인한 명령과 달랐다 (코드 리뷰 1 · 자체 점검 1) — T-049

반입 가이드 10절 ③은 `up -d api`였고 컨테이너 확인은 `restart api`로 했다. 파일(`ca/ca.pem`)을 둔 것은 설정이 아니라 `up -d`가 알아채지
못한다 — https + 키 없는 LLM(①을 건너뛰어 `.env`가 그대로)이면 **절차대로 해도 앱이 인증 기관을 모르고**, 문제 표가 같은 자리로 되돌려
보낸다. CLAUDE.md 4.1절("적은 그대로 실행해 확인한다")을 어겼다. 같은 자리에서 코드 리뷰가 하나 더 찾았다(3) — 묶음에 `ca/`가 없으면 도커가
root 소유로 만든다. 실측으로 확인했다.

### ③ 접근 로그가 라우터와 다르게 가정했다 (자체 점검 2 · 보안 검토 2) — T-048

맞춘 것이 없는 API 요청은 `route: "{*any}"`로 남고(정적 자산의 전체 잡기 라우트), 대문자 경로(`/API/…`)는 처리기·가드를 다 지나고도 접근
로그에만 없었다. 둘 다 단위 시험이 가정한 입력으로는 드러나지 않고 실제 앱에 보내야 보였다 — 두 검토가 따로 `:3100`에서 재현했다.

### ④ 읽고 다시 쓰는 곳 셋이 잠그지 않았다 (코드 리뷰 4 · 자체 점검 6 · 보안 후보 1)

역할 변경·위임·사내 계정 동기화가 읽은 `grants`를 다시 썼다. 역할을 그대로 두는 변경도 쓰므로 그 사이 root가 준 것이 사라지거나 거둔 것이
되살아났다. 셋 다 `SELECT … FOR UPDATE`로 읽는다. **시험이 정말 잡는지 봤다** — 잠금만 빼면 동시성 시험 셋이 모두 실패했다(검증기록 3절).

## 3. 처리

| # | 검토 | 지적 | 처리 |
|---|---|---|---|
| S1 | 보안 (MEDIUM) | 위임 없는 관리자가 위임받은 관리자를 초기화해 넘겨받는다 | **반영** — ①. `packages/shared/src/permissions.ts`, 사용자 관리 화면. 시험: A등급(Red `015ddce`), 통합(초기화·역할·잠금 해제·세션 종료가 403, 같은 것을 가진 관리자·root는 된다), 컴포넌트 |
| S2 | 보안 (LOW) | 대문자 경로가 접근 로그에 없다 | **반영** — ③ |
| S-후보1 | 보안 | 거둔 위임이 되살아나는 경합 | **반영** — ④ |
| S-후보2 | 보안 | 앱에 바로 붙으면 클라이언트가 고른 `X-Request-Id`를 쓴다 | **두지 않음** — 설계대로다(A.1-6: nginx가 없는 개발에서도 번호가 잇게). 운영은 nginx가 덮어쓰고(`proxy_set_header`) api 포트를 열지 않는다(`expose`만). 컨테이너에서 클라이언트 값이 로그 어디에도 없음을 확인했다(검증기록 2.2) |
| S-후보3 | 보안 | 아무 관리자나 역할을 내렸다 올려 위임을 거둔다 | **S1로 막혔다** — 위임받은 관리자의 역할은 root(와 같은 것을 가진 관리자)만 바꾼다 |
| S-후보4 | 보안 | connect-pg-simple의 정리 타이머가 첫 요청의 문맥을 쥔다 | **두지 않음** — 그 타이머의 로그는 `console.error`로 가서 우리 로거의 요청 문맥(mixin)을 지나지 않는다. 그 로그를 pino에 잇는 날 볼 것 — 검증기록 9절 |
| S-밖 | 보안 | 시드가 root로 되돌릴 때 위임을 비우지 않는다 | = C2 |
| C1 | 코드 리뷰 | 파일만 둔 뒤 `up -d`는 아무것도 하지 않는다 | **반영** — ②. 가이드·`deploy/ca/README.md`를 `up -d --force-recreate api`로 |
| C2 | 코드 리뷰 | 시드가 위임을 비우지 않아 CHECK에 걸린다 | **반영** — `apps/api/src/db/seed.ts`. 시험 DB에서 재현: 고치기 전 `users_grants_admin_chk`로 실패, 고친 뒤 `root 계정 보정: … role, grants, approvedAt` |
| C3 | 코드 리뷰 | 묶음에 `ca/`가 없어 도커가 root로 만든다 | **반영** — ②. 필수 파일 `ca/README.md`, 묶기·검사가 하위 디렉토리를 본다 |
| C4 | 코드 리뷰 | 역할 변경이 잠그지 않은 스냅숏으로 위임을 쓴다 | **반영** — ④ |
| C5 | 코드 리뷰 | 위임할 수 있는 행위의 목록이 세 곳에 따로 있다 | **반영** — drizzle 스키마의 CHECK는 코드의 목록에서 만들고, 마이그레이션의 CHECK와 코드의 목록이 같은지 시험이 본다(`apps/api/src/db/constraints.integration.spec.ts`). 마이그레이션은 손으로 쓴 SQL이라 목록 자체는 두 곳에 남는다(보류 17) |
| C6 | 코드 리뷰 | 요청 안에서 남은 줄의 `userId`(대상)가 부른 사람을 덮는다 | **반영** — 대상은 `targetUserId`(`collab.revoked`·`session.revoke_failed`). 설계서 D.4, 가이드 7.28 |
| C7 | 코드 리뷰 | 모의 발송 줄이 제목(부른 사람의 이름)을 싣는다 | **반영** — 제목을 싣지 않는다. Phase 11 전부터 문장에 있던 것이지만 7절("사용자는 불투명 ID로")에 어긋난다 |
| C8 | 코드 리뷰 | `mail.mention_failed`가 우리 쪽 결함인데 warn · 사내 IdP 실패에 event가 없다 | **반영** — error로. IdP는 `auth.oidc_failed`(warn) + 닿지 않으면 502(설계서 A.1-17). 통합 시험 셋(닿지 않음·우리가 판정한 거절·state 불일치는 남기지 않음) |
| C9 | 코드 리뷰 | 위임이 대상의 상태를 보지 않는다 · 같은 목록에도 감사 행을 남긴다 | 같은 목록: **반영** — 쓰지도 남기지도 않는다(A.1-15). 상태: **두지 않음** — 역할도 상태와 상관없이 바뀐다. 잠기거나 승인 전인 사람은 로그인하지 못해 위임을 쓰지 못하고, 풀리면 관리자로서 가진 것을 그대로 가진다 |
| C10 | 코드 리뷰 | `request_id`에 인덱스가 없고 화면·API에서 거를 수 없다 | 거르기: **반영** — 감사 조회 `requestId`, 목록에 번호, 관리 화면의 "요청 번호" 칸(A.1-18). 인덱스: **두지 않음** — 100만 행에서 66~133ms(검증기록 2.8). 되돌릴 조건은 설계서 A.1-18 |
| R1 | 자체 점검 (높음) | = C1 | 반영 |
| R2 | 자체 점검 (보통) | 없는 API 주소가 `{*any}`로 남는다 | **반영** — ③ |
| R3 | 자체 점검 (보통) | `verify:docs`가 `LOG_EVENTS`를 빌드 산출물에서 읽는다 — 혼자 돌리면 옛 목록 | **반영** — 스크립트가 shared를 먼저 빌드한다(다른 스크립트와 같은 모양) |
| R4 | 자체 점검 (보통) | = C3 | 반영 |
| R5 | 자체 점검 (낮음) | = C2 | 반영 |
| R6 | 자체 점검 (낮음) | = C4 | 반영 |
| R7 | 자체 점검 (낮음) | 기동의 두 event가 날것의 문자열이다 | **반영** — `satisfies LogEvent`(목록 밖의 코드는 컴파일이 막는다) |
| R8 | 자체 점검 (낮음) | = C10 (인덱스) | 두지 않음 — C10 |
| R9 | 자체 점검 (낮음) | CLAUDE.md 4.1절의 `verify:docs` 설명에 event 대조가 없다 | **반영** |
| R10 | 자체 점검 (낮음) | 검증기록의 CI 칸이 비었다 | **반영** — 병합 직전 값으로 채운다 |
| R11 | 자체 점검 (낮음) | "event 없는 줄은 기동할 때" — 실행 중에도 나올 수 있다 | **반영** — 가이드 7.28의 문장 |

## 4. 반영 뒤 확인

수치는 [`docs/P11_검증기록_Ops.md`](../P11_검증기록_Ops.md)에 있다 — 반영 뒤의 시험 수·커버리지·E2E, 가이드의 명령을 **적힌 그대로** 다시 돌린
컨테이너 확인(2.5), 반입 묶음을 실제로 만들어 풀어 본 것(2.9), 인덱스 실측(2.8).

## 5. 종료 루틴 — 반영분에

(아래에 적는다)
