# Scope Definition (v1)

- 상위 규칙: [`CLAUDE.md`](../CLAUDE.md) (충돌 시 규칙서 우선)
- 구현 방법: [`docs/설계서_Architecture.md`](설계서_Architecture.md)
- 요청 기록: [`docs/prompts/`](prompts/) 아래 사용자 요청 원문 (`CLAUDE.md` 11절)
- 작성일: 2026-09-16 / 작성 LLM: Claude Opus 5
- 상태: **Phase 11까지 완료** (2026-09-26)

> 이 문서는 **무엇을 왜 만드는가**를 정한다. 어떻게 만드는가는 `설계서_Architecture.md`, 어떤 규칙으로 만드는가는 `CLAUDE.md`다.
> 판단이 바뀌면 지우지 않고 머리에 개정 사유를 남긴다 (`CLAUDE.md` 10절).

## 1. 프로젝트 개요

**금융 폐쇄망 안에서 쓰는 위키·문서 협업 시스템**을 만든다. 팀이 업무 문서를 함께 쓰고, 고쳐 온 이력을 잃지 않으며, 누가 무엇을 언제 바꿨는지 추적할 수 있어야 한다.

### 1.1 왜 직접 만드는가

상용 위키를 도입하는 대신 자체 구축하는 이유는 세 가지다.

| 이유 | 내용 |
|---|---|
| **반입 제약** | 폐쇄망은 인터넷이 없다. 상용 제품은 설치·갱신·라이선스 확인 경로가 전부 반입 절차를 타야 하고, 보안 패치 주기를 우리가 통제할 수 없다 |
| **감사 요구** | 금융기관은 누가 무엇을 조회·변경했는지 증적이 필요하다. 감사 항목과 보존 정책을 제품 기능에 맞추는 것이 아니라 우리 정책에 맞춰야 한다 |
| **인증 연동** | 사내 IdP(OIDC)와 로컬 계정을 함께 쓴다. 상용 제품의 연동 방식에 사내 IdP를 맞추는 것보다 우리가 IdP에 맞추는 편이 현실적이다 |

### 1.2 대상과 규모

- 사용자 약 **300명**. 동시 접속은 수십 명 수준으로 가정한다.
- 단일 앱 서버 + PostgreSQL 1대로 시작한다. **이중화는 하지 않는다** — 부하 실측으로 판단이 끝났다 (`CLAUDE.md` 보류 6 닫음, `P5_검증기록_Release` 4절).
- 문서량은 초기 수천 건, 첨부 포함 수십 GB 이내로 가정한다.

### 1.3 제품 기준

기능의 기준점은 **Confluence의 실제 동작**이다. 다만 그대로 베끼지 않는다. 기능이 있는 Phase에서는 그 기능을 Confluence와 대조해 채택·변형·제외를 **그 Phase 설계서의 대조 절**에 남기고, 본문에는 우리 판단의 근거만 쓴다 (`CLAUDE.md` 1.3절·4절).

## 2. 용어

| 용어 | 뜻 |
|---|---|
| **스페이스** | 페이지를 담는 최상위 구획. **개인**(만든 사람만)과 **팀**(Crew만) 두 종류 |
| **카테고리(분류)** | 스페이스를 묶는 이름표. 사용자가 만든다 |
| **Crew** | 팀 스페이스를 함께 쓰는 사람들. `owner`(생성자) · `editor`(편집) · `viewer`(보기) |
| **페이지** | 스페이스 안의 문서. 부모-자식 트리를 이룬다 |
| **버전** | 페이지를 저장할 때마다 쌓이는 스냅샷. 지워지지 않는다 |
| **역할** | 시스템 권한. `root`(시스템) ⊃ `admin`(사용자·스페이스 관리) ⊃ `member`(일반) |
| **사용자 상태** | `승인 대기` → `활성`. 로그인 실패가 쌓이면 `잠김` |
| **스페이스 상태** | `활성` / `중지`. 중지되면 읽기 전용 |

## 3. 제약

| 구분 | 제약 | 영향 |
|---|---|---|
| 망 | 운영은 인터넷 차단 | 런타임에 외부 자원을 **하나도** 쓰지 않는다. 폰트·아이콘·문서 UI 자산 전부 번들 |
| 반입 | 파일 반입 절차를 거친다 | 이미지 tar + 체크섬 + SBOM·라이선스 목록을 묶음으로 전달. 이미지 크기를 작게 유지한다 |
| 개발 | 개발·빌드가 사내 Linux 서버 한 대 (2026-09-21 통합). 다른 프로젝트와 공유 | 명령은 OS 무관한 `pnpm` 스크립트로만. 남의 컨테이너·볼륨을 건드리지 않고, 프록시 뒤라 빌드에 프록시를 넘긴다 |
| 보안 | 금융기관 내부 시스템 | 기본 거부 권한, 감사로그 append-only, 세션 서버측 통제, 의존성 라이선스·시크릿 검사 |
| 저장소 | GitHub **공개** | 사내 호스트명·IP·계정·IdP 등록값·실데이터를 커밋하지 않는다 (`CLAUDE.md` 12.3절) |

## 4. 기능 범위

### 4.1 In Scope

| # | 기능 | 내용 | Phase |
|---|---|---|---|
| 1 | 로컬 계정 | 가입 요청 → 관리자 승인 → 활성. 비밀번호 정책·실패 잠금·변경 | 1 |
| 2 | 계정 복구 | ID 찾기(email+이름 → 마스킹 ID), PWD 초기화 **요청**(관리자가 처리), 관리자 초기화 | 1 |
| 3 | IdP 연동 | 사내 OIDC Authorization Code. 사용자·그룹 JIT 동기화 | 1 |
| 4 | 세션 | 서버측 세션, 유휴·절대 타임아웃, 비밀번호 변경·초기화 시 전 세션 파기 | 1. 관리자 강제 종료도 있다 (`POST /api/users/:id/terminate-sessions`, FR-539). ~~아직 없다~~ → **정정 2026-09-22: Phase 4에서 만들었다** (`P4_검증기록_Admin` 6절) |
| 5 | 역할·권한 | root/admin/member, 기본 거부, 판정 단일 지점 | 1. ~~세분화 4~~ → **하지 않는다** (P4 쟁점 4, 아래 4.2절) |
| 6 | 감사로그 | append-only, 인증·권한·문서·관리 작업 기록, 관리자 조회 | 1 (조회 화면 4) |
| 7 | 스페이스 | 개인/팀, 카테고리, 상태(활성/중지), 생성·수정·삭제 규칙 | 2. ~~권한 세분화 4~~ → **하지 않는다** (같은 이유) |
| 8 | Crew | 팀 스페이스 구성원 추가·역할 변경·제외 | 2 |
| 9 | 페이지 | 계층 트리, 이동, soft delete | 2 |
| 10 | 편집기 | WYSIWYG(제목·목록·표·코드·인용·링크), 서버측 문서 검증 | 2. **실시간 동시 편집 6** (Yjs. 편집의 기본 경로가 됐다). **실시간 편집의 관문 9** (조작한 변경은 적용 전에 거절, 편집기와 허용 목록을 테스트로 맞춘다) |
| 11 | 버전 | append-only 이력, 조회, 복원, 저장 충돌 감지(409) | 2. **버전 비교(diff) 6** |
| 12 | 검색 | 한글 부분 일치, 접근 가능한 스페이스로 제한 | 3 |
| 13 | 첨부 | 업로드·다운로드, 내용 해시 저장, 형식·크기 제한 | 3 |
| 14 | 댓글·멘션·라벨 | 페이지 댓글, @멘션 기록, 라벨 분류 | 댓글 3 / **멘션·라벨 4** (P3는 라벨 테이블만 만들었다) |
| 15 | 관리 콘솔 | 사용자·스페이스·감사로그 관리 화면 | 4 |
| 16 | 휴지통·복원 | 삭제한 페이지·스페이스 복원, 보존 기간 후 정리 | 4 |
| 17 | 템플릿·내보내기 | 페이지 템플릿, HTML 내보내기(인쇄 CSS) | ~~4~~ → **6**. Phase 4가 손대지 않았고 착수 쟁점에도 올리지 않았다. 인수 기준(5절)에도 없어 **Phase 4를 닫아도 확인되지 않는다** — 미루는 것을 여기 적어 사라지지 않게 한다 (2026-09-22) |
| 18 | 알림 | 앱 내 알림함. **멘션만** — 권한 변경 알림은 만들지 않는다 (P4 쟁점 2: 요구가 확인되지 않았고 켜는 순간 알림이 소음이 된다) | 4. **사내 메일 발송 6** (`WF_MAIL_ENABLED`. 실연동 확인은 보류 18). **같이 쓴 문서의 멘션 귀속 8** (보류 21) |
| 19 | 운영 | 배포·운영이관 가이드, 백업·복원, 부하·보안 점검, 반입 번들 | 5 |
| 20 | 사내 LLM 질문 | 시스템 관리자가 등록한 사내 LLM(vLLM)에 메뉴에서 묻는다. 답은 흘러나오고, 사람마다 지시문을 여럿 저장해 고르며, 대화는 기간·개수만큼 남는다(고정한 것은 풀 때까지). 위키 페이지는 텍스트·마크다운으로 **복사해 붙인다** — 서버가 페이지를 LLM에 보내지 않는다 (`docs/기능백로그.md` F-002) | 10. 사내 LLM 실연동 확인은 보류 29 |

### 4.2 Out of Scope

| 항목 | 사유 | 재검토 시점 |
|---|---|---|
| ~~실시간 동시 편집~~ | ~~1단계는 편집 잠금 + 저장 충돌 감지로 충분하다~~ → **Phase 6에서 만들었다 (2026-09-22).** JSON을 정본으로 두고 Yjs 상태는 파생 데이터로 분리해 저장 모델의 복잡도를 가뒀다 | 완료 |
| PDF 내보내기 | 헤드리스 브라우저를 이미지에 넣으면 반입 용량이 수백 MB 늘어난다 → **재검토 끝 (2026-09-22): 하지 않는다.** HTML + 인쇄 CSS를 Phase 6에서 만들었다. 브라우저의 "인쇄 → PDF로 저장"이면 결과가 같다 | 종료 |
| Confluence 데이터 가져오기 | ~~이관 대상 데이터의 존재·형식이 확정되지 않았다~~ → **닫음 (2026-09-22, 사용자): 이관하지 않는다.** 새로 시작한다 | 종료 |
| ~~외부 알림(메일)~~ | ~~발송 경로가 확정되지 않았다~~ → **Phase 6에서 만들었다 (2026-09-22).** 사용자가 "메일 전송 API가 있다"고 확인해 줬다. 메신저는 여전히 밖이다. 실연동 확인은 보류 18 | 완료(메일) |
| 페이지 단위 세밀 권한(상속·예외) | 스페이스 단위로 시작해 실제 요구를 본 뒤 넓힌다 | ~~Phase 4~~ → **재검토 끝 (2026-09-22): 하지 않는다.** 그 요구가 확인되지 않았고, 요구 없이 넓히면 판정 코드가 두 배가 되고 되돌리기 어렵다. 실제 요구가 오면 그때 (`기능백로그.md`로) |
| 다국어 UI | 사내 사용자는 한국어 단일 | 요구 발생 시 |
| 모바일 전용 화면 | 데스크톱 브라우저 기준. 반응형은 최소한만 | 요구 발생 시 |
| 전문 검색엔진 도입 | PostgreSQL 내장으로 시작. 반입 표면을 늘리지 않는다 | ~~Phase 3 측정 후~~ → **측정 끝 (2026-09-22).** 도입하지 않는다. 근거는 `P3_검증기록_Content` 2절 |

## 5. Phase 로드맵과 인수 기준

각 Phase는 **브라우저에서 동작하는 증분**으로 끝난다. 인수 기준은 사람이 화면에서 확인할 수 있는 문장으로 쓴다.

| Phase | 명칭 | 인수 기준 (이것이 되면 그 Phase는 끝났다) |
|---|---|---|
| **0** | 범위·아키텍처·공통 기반 | Linux 서버에서 pull → 이미지 빌드 → compose 기동 → 브라우저에서 앱이 뜨고 `/api/health`가 `db: ok`를 반환한다. `pnpm check`가 통과한다 |
| **1** | 인증·사용자·권한 골격 | 가입 요청한 계정을 관리자가 승인하면 로그인된다. IdP 계정으로 로그인하면 그룹이 역할로 매핑된다. 두 경로 모두 감사로그에 남는다 |
| **2** | 스페이스·페이지·편집기 | 팀 스페이스를 만들고 Crew를 넣고, 페이지를 작성·편집·이동하고, 이전 버전을 복원할 수 있다. ~~두 사람이 같은 버전을 저장하면 뒤가 충돌 안내를 받는다~~ → **정정 2026-09-22 (Phase 6):** 실시간 편집이 기본 경로가 되어 화면에서는 이 상황이 생기지 않는다. 기준 버전 불일치 409는 REST 저장에 그대로 있고 `P6_검증기록_Collab` 8절이 API 수준에서 확인한다 |
| **3** | 검색·첨부·댓글 | 한글 두 글자로 페이지가 검색되고, 내가 볼 수 없는 스페이스는 결과에 없다. 파일을 첨부·다운로드하고 댓글을 단다 |
| **4** | 권한 세분화·관리·알림 | 관리자가 사용자·스페이스를 관리하고 감사로그로 추적한다. 삭제한 페이지를 휴지통에서 되살린다. 멘션이 알림함에 뜬다 |
| **5** | 운영화·반입 | 반입 리허설: 이미지 tar를 옮겨 기동하고, 백업본으로 복원하며, 300명 기준 부하에서 목표 지연을 만족한다 |
| **6** | 2단계 (실시간 편집·비교·내보내기·템플릿·메일) | 두 브라우저에서 같은 페이지를 열어 한쪽이 치면 다른 쪽에 바로 보이고, 둘 다 닫으면 버전이 하나 남는다. 두 버전을 골라 무엇이 바뀌었는지 본다. 페이지를 HTML로 내려받아 브라우저에서 인쇄하면 쓸 만한 문서가 나온다. 템플릿으로 페이지를 시작한다. 멘션이 알림함과 메일 둘 다로 간다 |
| **7** | 반입 전 강화 | 편집 화면을 열어 둔 채 **같은 브라우저의 다른 탭에서 로그아웃**하면 그 편집 연결이 끊긴다(로그아웃은 누른 세션 하나의 일이다). 관리자가 세션을 강제 종료하면 그 사람의 편집 연결이 **모두** 끊긴다. 혼자 5분 넘게 편집해도 끊기지 않는다. 접속만 하고 아무것도 고치지 않은 사람이 버전 작성자가 되지 않는다. app 이미지가 예산 400MB 안에 여유를 두고 들어간다 |
| **8** | 멘션 귀속 (보류 21) | 두 브라우저에서 같은 페이지를 열어 A가 `@B의아이디`를 치고, **그 뒤에 B가 다른 곳을 고쳐 마지막 작성자가 된다.** 편집이 멈춰 버전이 남으면 B의 알림함에 "A 님이 불렀다"가 뜬다(예전에는 B 자신의 이름). 남이 A의 글자를 지우거나 끼워 넣어 새로 생긴 멘션은 A의 이름으로 나가지 않는다 |
| **9** | 실시간 편집의 관문 (보류 22·23·24) | 두 브라우저에서 같은 페이지를 열어 둔 채, **조작한 연결**이 편집기가 만들지 않는 노드(`Y.XmlHook`)나 보이지 않는 속성(`onclick`)을 보내거나 B의 클라이언트 ID로 먼저 쓴다. 그 연결은 끊기고 보낸 것은 문서와 저장본에 없으며, A와 B는 계속 서로의 편집을 보고 자동 저장이 이어진다. 거절은 감사로그에 남는다. 편집기에 이메일 주소를 치거나 정렬된 표·제목 붙은 링크를 붙여 넣어도 저장된다 |
| **10** | 사내 LLM 질문 (F-002 · 보류 28) | 시스템 관리자가 관리 화면에서 사내 LLM을 등록하면 곧바로 연결이 확인되고, **API 키는 다시 보이지 않는다.** 일반 사용자가 "LLM 질문"에서 지시문을 골라 물으면 **답이 끝나기 전부터 흘러나오고**, 중지하면 거기까지 남는다. 대화를 고정하고 이어 물으면 앞 대화를 이어 받는다. 위키 페이지를 "마크다운 복사"해 질문 칸에 붙여 물을 수 있다. 목록은 "고정 n/20 · 보관 n/100 · 7일"을 말한다 |
| **11** | 운영 로그·LLM 관리 위임·사내 LLM 반입 설정 (F-003 · F-004 · 보류 29) | 운영자가 오류 한 줄의 **요청 식별자**로 같은 요청의 nginx 로그·앱 로그·감사로그 행을 찾는다. 로그 줄마다 **event 코드**가 있고 장애대응 가이드가 그 코드로 찾게 한다. 검색어가 로그에 남지 않는다. 로그가 서비스마다 정해진 크기에서 순환한다. 시스템 관리자가 사용자 관리에서 관리자 한 사람에게 **LLM 연결 관리**를 주면 그 관리자가 LLM을 등록할 수 있고, 거두면 다음 요청부터 막힌다. 사내 LLM은 반입 가이드의 "사내 LLM 연결하기"만 보고 현장에서 연결한다 — compose를 고치지 않는다 |

## 6. 비기능 목표

| 항목 | 목표 | 측정 방법 |
|---|---|---|
| 응답 지연 | 동시 50세션에서 주요 화면 p95 **1초 이내** | Phase 5 부하 테스트 |
| 이미지 크기 | app 이미지 **400MB 이하** | Linux 빌드 시 `docker images` 실측 |
| 기동 시간 | compose 기동 후 30초 이내 헬스체크 통과 | Phase 0 빌드 검증 |
| 가용성 | 서버 재부팅 후 자동 기동 | `restart: unless-stopped` + 실제 재부팅 확인 |
| 복구 | 백업본으로 데이터·첨부 복원 가능 | Phase 5 복원 리허설 |
| 테스트 | A등급 ≥90%, api B등급 ≥70%, 가중 평균 ≥80% | `pnpm test:cov` 실측 |
| 외부 의존 | 런타임 외부 URL 참조 **0건** | CI에서 빌드 산출물 검사 |
| 감사 | 인증·권한·문서 변경이 빠짐없이 기록되고 수정·삭제 불가 | DB 트리거 + 권한, Phase 1 테스트 |

## 7. 확정된 결정과 근거

프로토타입(`exp/prototype`)에서 화면으로 확인한 뒤 확정한 것들이다. 프로토타입은 결정의 **계기**이고, 아래는 우리가 그 결정을 유지하는 **이유**다.

### 7.1 계정

| 결정 | 이유 |
|---|---|
| 비밀번호 **8자 이상·2종 조합**, 5회 실패 시 15분 잠금 | 사내망 안에서만 접근되는 문서 시스템이다. 길이·복잡도를 과하게 올리면 사용자가 메모에 적어 두는 쪽이 더 큰 위험이 된다. 잠금은 유지해 무차별 대입을 막는다. 사내 정책 문서가 확인되면 그 값으로 바꾼다 (`CLAUDE.md` 확인 필요 B) |
| 가입은 **요청 → 관리자 승인** | 누가 시스템에 들어오는지를 관리자가 통제해야 한다. 자동 가입은 폐쇄망이라도 퇴직자·외부 인력 문제를 남긴다 |
| 승인 시 **개인 스페이스 자동 생성** | 첫 로그인에 아무것도 없으면 무엇을 해야 할지 모른다. 바로 쓸 공간을 하나 준다 |
| ID 찾기는 **email + 이름** 둘 다 요구하고 **마스킹된 ID**만 보여준다 | email만으로 ID를 돌려주면 email 목록을 가진 사람이 계정을 열거할 수 있다. 두 정보를 요구하고 결과를 가려 노출 범위를 줄인다 |
| ~~PWD 찾기는 임시 비밀번호를 화면에 1회 표시한다~~ → **정정 2026-09-21.** 미인증 화면에서는 발급하지 않는다. 요청만 남기고 **관리자 초기화**로 처리한다. 임시 비밀번호의 1회 표시는 관리자 화면에서만 | 아이디와 사내 email은 사실상 공개 정보라 "둘을 아는 사람 = 본인"이 성립하지 않는다. 당시에는 메일이 없어 대역 외 확인 수단이 아예 없었다. **Phase 6에서 사내 메일 발송이 생겼으므로 이 근거는 더 이상 성립하지 않는다 (2026-09-23).** 그래도 결정은 유지한다 — 자가 재설정은 메일 도달만으로 본인을 확인하는 흐름이라 별도 판단이 필요하고, 사내 메일 API는 아직 실연동 확인 전이다(보류 18). 다시 볼 거리로 `docs/기능백로그.md`에 올렸다 (`P1_설계서_Auth` 2.4절) |
| 담당자 확인에 **email을 노출하지 않는다** | 로그인 전 화면이다. 연락처는 사내 다른 경로로 이미 알 수 있고, 여기 두면 수집 대상이 된다 |
| 비밀번호 입력에 **눈 아이콘**(기본 가림) | 오타로 인한 실패·잠금을 줄인다. 기본이 가림이어야 어깨너머 노출을 막는다 |

### 7.2 스페이스

| 결정 | 이유 |
|---|---|
| **개인 / 팀** 두 종류 | 혼자 쓰는 메모와 함께 쓰는 문서는 접근 통제가 다르다. 한 종류로 두면 "나만 보는 공간"을 만들 방법이 없다 |
| 팀 스페이스는 **Crew만** 접근 | 사내 전체 공개가 기본이면 부서 문서가 의도치 않게 퍼진다. 필요한 사람을 명시적으로 넣는다 |
| Crew 역할 **owner / editor / viewer** | "함께 작업하는 사람"과 "볼 수만 있는 사람"이 실제로 나뉜다. owner는 구성원을 관리한다 |
| 관리자는 **모든 스페이스를 본다** | 사용자 이탈·분쟁·감사 시 관리자가 접근하지 못하면 운영이 막힌다. 접근은 감사로그에 남는다 |
| 화면에서 **키(식별자)를 없애고 분류(카테고리)를 쓴다** | 사용자가 키를 정하면 중복·의미 없는 문자열이 쌓인다. 식별자는 시스템이 만들고, 사람은 분류로 찾는다 |
| 카테고리는 **모든 활성 사용자가 생성** | 스페이스를 만드는 사람이 분류도 만들 수 있어야 흐름이 끊기지 않는다. 남용되면 Phase 4에서 관리 기능을 붙인다 |
| 상태 **활성 / 중지**, 중지는 읽기 전용 | 쓰지 않는 스페이스를 지우기 전에 얼려 둘 단계가 필요하다. 문서는 남기고 편집만 막는다 |
| 삭제는 **생성자는 Crew가 본인뿐일 때**, **관리자는 중지 상태일 때** | 여러 사람이 쓰는 공간을 한 사람이 지울 수 있으면 안 된다. 관리자 삭제도 "중지"라는 예고 단계를 거친다 |

### 7.3 역할

| 결정 | 이유 |
|---|---|
| **root ⊃ admin ⊃ member** 3단계 | 시스템 운영(설정·버전·기반)과 업무 관리(사용자·스페이스)는 책임이 다르다. 한 역할로 묶으면 업무 관리자에게 시스템 권한까지 준다 |
| root만 root를 부여한다. admin은 root를 관리할 수 없다 | 권한 상승 경로를 막는다 |
| **자기에게 없는 위임을 가진 관리자는 관리하지 못한다** (Phase 11) — 승인·비밀번호 초기화·역할·잠금 해제·세션 종료 | 같은 판단이다 — 위임받은 관리자의 비밀번호를 초기화하면 그 계정으로 위임을 얻는다(병합 전 보안 검토). 역할의 우열과 같이 가진 것의 우열을 본다 |
| 권한 판정은 **공유 패키지의 순수 함수 한 곳** | 화면·API·가드가 각자 판단하면 언젠가 어긋난다. 한 곳에서 판정하고 테스트로 고정한다 |

### 7.4 문서

| 결정 | 이유 |
|---|---|
| 본문은 **구조화 JSON만** 받는다. HTML을 받지 않는다 | HTML을 받으면 서버가 정화(sanitize)해야 하고, 정화 규칙의 구멍이 그대로 XSS가 된다. 허용 노드·마크 목록 밖은 거부하는 편이 확실하다 |
| 버전은 **append-only**, 복원도 새 버전 | 이력이 지워지면 감사가 불가능하다. 복원조차 "되돌린 사실"을 남긴다 |
| 저장 시 **기준 버전 불일치는 거부(409)** | 마지막 저장이 이기면 앞사람 작업이 조용히 사라진다. 사용자에게 알리고 선택하게 한다 |
| 삭제는 **soft delete** | 실수로 지운 문서를 복구할 수 있어야 한다 |
| 실시간 편집의 변경도 **적용하기 전에** 같은 허용 목록으로 본다. 어긋나면 **받지 않고 그 연결을 끊는다** (P9 쟁점 1, 사용자 결정 2026-09-24) | 받은 뒤 서버가 고치는 길은 정상 편집이 잘못 걸렸을 때 그 내용을 모두의 화면에서 조용히 지우고, 남이 차지한 클라이언트 ID(보류 24)는 되돌리지 못한다. 끊기면 그 사람이 알고, 기록이 까닭을 말한다 |
| **편집기 스키마와 허용 목록은 같다** — 대조 테스트가 강제한다 (P9 D.7) | 편집기가 만드는 것이 허용 목록 밖이면 정상 편집이 끊기고, 허용 목록에만 있는 것은 조작한 클라이언트만 넣는 보이지 않는 내용이 된다. 이메일 주소 하나로 저장이 멈추던 것(Phase 6부터)이 이 어긋남이었다 |

### 7.5 정정 이력

- 없음 (v1 최초 작성).

### 7.6 사내 LLM (Phase 10, 사용자 결정 2026-09-25)

| 결정 | 이유 |
|---|---|
| 대화는 **기간 한정 저장** — 고정하지 않은 대화는 마지막 사용 뒤 7일, 사람마다 100개(고정 포함), 고정은 20개까지 풀 때까지 | 질문에 업무 내용이 들어간다. 무기한이면 문서 본문만큼 무거운 기록이 끝없이 쌓이고, 저장하지 않으면 이어 묻지 못한다. 세 값은 운영 설정이라 사내 보존 정책이 나오면 그 값으로 바꾼다 |
| **위키 내용을 질문에 자동으로 넣지 않는다** — 페이지에서 복사해 붙인다 | 무엇이 사내 다른 서버로 가는지 **사람이 보고** 정한다. 페이지 권한 판정을 LLM 경로에 다시 세울 일도 없다 |
| API 키는 **DB에 암호화**해 두고 다시 보여 주지 않는다 | 관리 화면에서 등록·삭제하려면 DB에 있어야 하고, 평문이면 DB 백업이 곧 키 유출이다 |
| **활성 사용자 전원**이 쓰고, 등록·삭제는 시스템 관리자 | 요청 원문. 시스템 관리자가 root만인지는 `CLAUDE.md` 확인 필요 F |
| 답을 **흘려보낸다** | 긴 답을 다 받을 때까지 빈 화면을 보면 멈춘 줄 안다. 중지할 수도 있어야 한다 |
| LLM 연결 관리는 **root가 늘 하고, 관리자 한 사람씩 위임**한다 (Phase 11) | 사용자 결정(2026-09-26) "시스템 관리자가 메인 권한을 갖고, 관리자에게 권한을 부여". 역할을 늘리지 않고 행위 하나를 사람에게 주면 누가 왜 가졌는지 사용자 관리 한 칸과 감사로그 한 줄로 끝난다 |
| 오류 한 줄을 **요청 번호로** nginx·앱·감사에서 함께 찾는다 (Phase 11) | 500 한 줄이 누구의 어느 요청인지 시각으로만 맞추면 사람 수가 늘수록 틀린다. 로그 줄마다 event 코드를 두어 문장을 고쳐도 가이드의 찾는 말이 깨지지 않는다 |
| 지시문(시스템 프롬프트)은 **이름을 붙여 여럿** 저장하고, 고치거나 지울 때까지 남는다 | 사람마다 쓰는 일이 여럿이다. 새 대화를 시작할 때 고르고, 그 대화에는 그때의 것이 복사된다 |

## 8. 위험과 대응

| # | 위험 | 대응 |
|---|---|---|
| 1 | 폐쇄망 반입 단계에서 이미지·의존성 문제가 드러나 설계가 뒤집힌다 | **Phase 0의 완료 기준을 Linux 빌드·기동으로 둔다.** 마지막이 아니라 처음에 뚫는다 |
| 2 | 한글 2글자 검색이 느리거나 결과가 빠진다 | ~~Phase 3에서 재현율·p95를 실측하고, 미달이면 확장(pg_bigm) 반입을 판단한다~~ → **닫힘 2026-09-22.** 실측 결과 50,000건에서 p95 66ms·재현율 1.0이다. 확장을 반입하지 않는다 (`P3_검증기록_Content` 2절) |
| 3 | IdP 실연동이 개발 PC에서 불가능해 늦게 터진다 | 모의 서버로 개발하되 **Linux 서버에서 실호출 검증**을 한다. 모의 통과는 완료가 아니다. → **정정 2026-09-21: 이 검증을 Phase 1 완료 기준에서 뺐다.** 작업 서버가 IdP에 나갈 수 없어(확인 필요 A) Phase 1을 무기한 열어 두게 되기 때문이다. 대신 보류 11로 등재하고 접근이 열리는 날 확인한다. **위험은 줄지 않았고 미룬 것이다** |
| 4 | 공개 저장소에 사내 정보가 섞인다 | CI에 gitleaks. 문서·예시는 placeholder만. 커밋 전 검사를 습관으로 |
| 5 | 프로토타입 코드를 근거 없이 승격해 품질 부채가 남는다 | 모듈 단위로 4단계 문서를 밟으며 승격. 근거를 쓸 수 없는 코드는 가져오지 않는다 |
| 6 | 문서가 실제 동작과 어긋난다 | `pnpm verify:docs`(기계 검사) + `doc-consistency` 에이전트(뜻 검사) 두 층 |
| 7 | 감사로그가 비대해져 성능·용량 문제를 만든다 | 보존 기간을 설정값으로 두고 Phase 4에서 정리 배치를 설계한다 |

## 9. 관련 문서

| 문서 | 관계 |
|---|---|
| [`CLAUDE.md`](../CLAUDE.md) | 상위 규칙 |
| [`docs/설계서_Architecture.md`](설계서_Architecture.md) | 이 범위를 어떻게 만드는가 |
| [`docs/P0_설계서_Foundation.md`](P0_설계서_Foundation.md) | Phase 0 요구사항과 설계 |
| [`docs/학습가이드_시스템이해.md`](학습가이드_시스템이해.md) | 개발 용어 없이 읽는 설명 |
| [`docs/운영가이드_장애대응.md`](운영가이드_장애대응.md) | 증상에서 조치로 |
| [`docs/기능백로그.md`](기능백로그.md) | 기능 요청 접수 상태 |
| [`docs/internal/검토서_방법론개정.md`](internal/검토서_방법론개정.md) | 왜 이런 작업 방식과 문서 구조인가 |
