# Phase 12 요청 원문

`CLAUDE.md` 11절. **사용자의 말을 그대로 둔다.** 요약하면 의도가 사라진다.

## 2026-09-26 · Phase 11 병합 뒤 "이어서 진행할 항목"에 대한 답

Phase 11(PR #10) 병합을 보고하고, 이어서 할 것을 묻자 보류 중 지금 할 수 있는 넷(30·27·25·16)을 제안했다. 사용자의 답:

> 보류 30 진행해줘, 제한사항은 없어. AI 서버에서 알아서 제한할거고 질문이 많으면 답변이 알아서 느려질거야. 다만, 질문하고, 응답이올때까지 답변을 기다리고 있다고 표시하고, 기다리는 시간을 초 단위(s)로 얼마나 기다리는지 표시했으면 좋겠어. 그리고 5초 이상 느려지면, "답변이 늦어지고 있습니다."도 같이 표시해줘.
> 25, 27번은 제안하는대로 해줘.
> 16은뭔지 모르겠는데, 설명해줄래?

- 보류 16은 **질문**이었다. 대화에서 설명했다(로그인할 때 느린 비밀번호 검사(argon2)를 데이터베이스 연결을 쥔 채 한다 — 로그인이 몰리면
  다른 요청이 연결을 기다린다. 고치려면 "없는 계정도 같은 시간"과 잠금 계산을 함께 지켜야 해 가볍지 않다). 정하지 않았으므로 보류로 남긴다.
- 나머지 셋은 Phase 12가 한다 — `docs/P12_설계서_Limits.md` A절.
