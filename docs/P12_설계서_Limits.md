# P12 설계서 — Limits (답을 기다리는 표시 · 문서 모양의 순서·개수 · 프레임과 값의 상한)

Phase 12는 Phase 11 뒤 사용자가 고른 세 보류를 닫는다. 요청 원문은 `docs/prompts/phase12/scope.md`, 요구사항 번호는 **FR-1300**,
비기능은 **NFR-120**부터다.

- **보류 30**(LLM 질문의 빈도 제한) → 제한을 두지 않고, 대신 **답을 기다리는 시간을 보인다**(기능백로그 F-005)
- **보류 25**(자식의 순서와 개수) — 편집기 스키마의 순서·개수 규칙을 정본에 들이고, 받는 편집기가 무엇을 하는지 잰다
- **보류 27**(프레임의 크기와 값의 범위) — 실시간 편집 한 프레임의 상한, 표 칸의 `colspan`·`rowspan`·`colwidth` 범위

선행: [`docs/P11_검증기록_Ops.md`](P11_검증기록_Ops.md) · 구조: [`docs/설계서_Architecture.md`](설계서_Architecture.md) · 관문: [`docs/P9_설계서_Gate.md`](P9_설계서_Gate.md)

> **경로에 백틱이 없는 것은 아직 만들지 않은 것이다** (4.1절). 만들면 붙인다.

---

## A. 착수 쟁점 (사용자 결정 2026-09-26)

| # | 쟁점 | 사용자의 답 | 이 설계에서의 뜻 |
|---|---|---|---|
| 1 | 보류 30 — 질문 빈도 제한을 둘까 | "제한사항은 없어. AI 서버에서 알아서 제한할거고 질문이 많으면 답변이 알아서 느려질거야." | **두지 않는다.** 한 사람이 동시에 하나만 묻는 규칙(P10 FR-1114)은 그대로다. 보류 30을 닫는다 |
| 2 | 기다리는 동안 무엇을 보이나 | "응답이올때까지 답변을 기다리고 있다고 표시하고, 기다리는 시간을 초 단위(s)로 … 5초 이상 느려지면, "답변이 늦어지고 있습니다."도 같이" | C.1 — 보낸 순간부터 첫 답 조각까지 `답변을 기다리고 있습니다 · Ns`, 5초부터 `답변이 늦어지고 있습니다.` |
| 3 | 보류 25·27 | "25, 27번은 제안하는대로 해줘." | 보류 표의 판정 방법대로 — 잰 결과로 정한 것이 A.1이다 |
| 4 | 보류 16 | "16은뭔지 모르겠는데, 설명해줄래?" | **질문이었다.** 설명했고 정하지 않았으므로 보류로 남긴다 |

## A.1 묻지 않고 정한 것 — 근거와 되돌릴 조건

| # | 정한 것 | 근거 | 되돌릴 조건 |
|---|---|---|---|
| 1 | 기다림은 **보내기를 누른 순간부터 첫 답 조각까지**다. 생각 과정(`thinking`)도 답 조각이다. 살아 있음 줄(`ping`)은 아니다 | "응답이 올 때까지" — 생각 과정은 화면에 흘러나와 기다림이 끝난 것이 보인다. 살아 있음 줄은 보이지 않는다 | 생각 과정 동안에도 세기를 원하면 끝을 `delta`로 |
| 2 | 초는 **내림한 정수**로 1초마다 바꾼다(`0s`부터). 5초는 설계 고정값 `LLM_TIMINGS.slowAnswerMs` | 사용자가 "초 단위(s)"로 말했다. 값을 바꾸면 문구의 뜻이 바뀌므로 운영 설정이 아니다 | — |
| 3 | 받는 편집기는 순서·개수를 어긴 요소를 **스스로 지우고 이웃은 남긴다**(C.2 실측) — 그래서 관문에서 막지 않는다. **정본이 편집기가 보는 모양을 적는다**: 변환이 편집기처럼 어긴 요소를 떨어뜨리고, 정본 검증은 그 규칙을 받는다 | 관문은 조각 하나씩 보므로 "끝난 뒤의 첫 자식"을 보려면 적용 뒤의 상태를 흉내 내야 한다(비용·복잡도). 잰 결과 남의 글은 지워지지 않고 편집기도 깨지지 않는다. 남는 위험은 서버가 편집기가 그리지 않는 모양을 정본에 남기는 것뿐이다 | 편집기(y-tiptap)를 올려 동작이 바뀌면 — 대조 시험과 C.2의 실측을 다시 한다 |
| 4 | 프레임 상한은 **16MiB**(`COLLAB_LIMITS.maxFrameBytes`, 지금은 `ws` 기본 100MiB). 넘으면 그 연결을 닫고(1009) warn 한 줄, 화면은 관문의 거절과 같게 말한다 | 가장 큰 정상 프레임은 접속 직후의 전체 상태다. REST 저장 상한(2MB JSON)의 문서가 Yjs로 약 2MB다(C.3 실측) — 편집 이력까지 8배를 둔다 | 실시간 상태가 커지는 것(보류 19)을 재 보고 |
| 5 | 표 칸의 `colspan`·`rowspan`은 **1~1000의 정수**, `colwidth`는 칸 수 이하의 배열이고 값은 비었거나 **1~10000의 정수**(`TABLE_LIMITS`). **편집기도 붙여 넣은 HTML을 이 범위로 줄인다** | 1000은 HTML 표준이 `colspan`을 읽는 상한이다 — 브라우저가 넘는 값을 1000으로 그린다. 편집기가 줄이지 않으면 큰 값을 붙여 넣은 정상 사용자가 관문에 끊긴다(P9의 원칙 "편집기가 만든 것은 서버가 받는다") | — |

## B. Confluence 대조 (`CLAUDE.md` 4절)

| Confluence의 것 | 우리 | 왜 |
|---|---|---|
| (Atlassian AI) 답을 만드는 동안 "생각 중" 표시 | **채택 — 기다린 초를 함께** | 사내 LLM은 붐비면 첫 조각까지 오래 걸린다(보류 29 판정 항목). 멈춘 것과 느린 것을 사람이 가를 수 있어야 한다 |
| 사용량 제한(요청 수) | **제외** | 사용자 결정 — 사내 LLM 서버가 자기 부하를 다룬다 |

---

## C. 요구사항

### C.1 답을 기다리는 표시 (FR-1300 ~ FR-1302, 보류 30 · F-005)

| # | 요구사항 | 근거 |
|---|---|---|
| FR-1300 | 질문을 보내면 첫 답 조각(생각 과정 포함)이 올 때까지 **`답변을 기다리고 있습니다 · Ns`**를 보인다 — N은 기다린 초(내림), 1초마다 | A-2, A.1-1·2 |
| FR-1301 | 기다림이 **5초 이상**이면 **`답변이 늦어지고 있습니다.`**를 함께 보인다 | A-2 |
| FR-1302 | 질문 빈도 제한을 두지 않는다 — 한 사람 동시 하나(FR-1114)는 그대로 | A-1 |

### C.2 문서 모양의 순서·개수 (FR-1310 ~ FR-1313, 보류 25)

| # | 요구사항 | 근거 |
|---|---|---|
| FR-1310 | 편집기 스키마의 순서·개수 규칙을 공유 규칙으로 둔다 — **비어 있을 수 없는 노드**(`doc`·`blockquote`·`bulletList`·`orderedList`·`listItem`·`table`·`tableCell`·`tableHeader`), **첫 자식**(`listItem`은 `paragraph`로 시작) | 편집기의 내용 식(C.2 표) |
| FR-1311 | 대조 시험이 편집기의 내용 식이 **정확히** "첫 자식 규칙 · 그 뒤 허용된 자식의 되풀이 · 비어 있음 여부"임을 증명한다 — 확장이 다른 순서 규칙을 들이면 시험이 먼저 깨진다 | 보류 25 판정 방법 |
| FR-1312 | 정본 검증(`validateDocument`)이 그 규칙을 받는다 — REST 저장·템플릿·복원 모두 | 7절 "편집기가 그리는 모양만" |
| FR-1313 | 실시간 상태 → 정본 변환이 **편집기처럼 읽는다** — 규칙을 어긴 요소는 떨어뜨리고(아래에서 위로), 비면 빈 문단 하나 | A.1-3 |

**편집기의 내용 식** (TipTap 3.31.3, `apps/web/src/components/extensions.ts`의 스키마에서 뽑았다):

```
doc = block+ · blockquote = block+ · bulletList = listItem+ · orderedList = listItem+ · listItem = paragraph block*
table = tableRow+ · tableRow = (tableCell | tableHeader)* · tableCell = block+ · tableHeader = block+
paragraph = inline* · heading = inline* · codeBlock = text*
```

**받는 편집기가 하는 일** (2026-09-26 실측, happy-dom에서 TipTap `Collaboration`으로 연 편집기):

| 받은 모양 | 편집기 | 공유 문서(Y) |
|---|---|---|
| 목록 항목이 인용·표로 시작 | 그 항목을 뺀 나머지를 그린다, 편집 됨 | **그 항목만 지운다** — 같은 목록의 남의 항목은 남는다 |
| 빈 목록 항목·빈 목록·빈 인용·빈 표 칸·줄 없는 표 | 그것을 뺀 나머지 | 그 요소를 지운다(비게 된 부모도) |
| 빈 문서(맨 위가 비었다) | 빈 문단 하나로 그린다, 입력 됨 | 그대로 — 누가 치면 문단이 생긴다 |
| 칸 없는 줄·줄마다 칸 수가 다름 | 그대로(표 정규화가 가린다) | 그대로 |

남의 글이 지워지거나 편집기가 깨지는 경우는 없었다 — 관문에서 막지 않는 까닭이다(A.1-3). 첫 로드 경로(`initProseMirrorDoc`)만으로 보면
"목록 항목이 표로 시작"에서 `check`가 실패하고 처음 자리 편집이 던졌지만, 열린 편집기는 같은 입력에서 요소를 지우고 이어 갔다.

### C.3 프레임과 값의 상한 (FR-1320 ~ FR-1323, 보류 27)

| # | 요구사항 | 근거 |
|---|---|---|
| FR-1320 | 실시간 편집 한 프레임은 **16MiB까지** 받는다. 넘으면 그 연결을 닫고 warn `collab.frame_too_large`(`pageId`·`userId`·상한) | A.1-4 |
| FR-1321 | 화면은 그 닫힘(1009)을 관문의 거절과 같게 말한다 — "서버가 이 편집을 받지 않았다" | P9의 거절 화면 |
| FR-1322 | 표 칸의 `colspan`·`rowspan`·`colwidth` 값은 A.1-5의 범위만 — 정본 검증과 관문이 같은 함수(`nodeAttrProblems`)로 본다 | A.1-5 |
| FR-1323 | 편집기가 붙여 넣은 HTML의 그 값을 같은 범위로 줄인다 — 대조 시험이 본다 | A.1-5 |

**프레임 크기의 근거** (2026-09-26 실측, `yDocFromDoc` → `Y.encodeStateAsUpdate`, 굵게·링크가 섞인 문단):

| 문단 | JSON | Yjs 전체 상태 |
|---|---|---|
| 500 | 167KiB | 169KiB |
| 2,000 | 671KiB | 687KiB |
| 6,000 | 2,015KiB (REST 상한 2MB 근처) | 2,070KiB |

### C.4 비기능 (NFR-120 ~ NFR-121)

| # | 요구사항 |
|---|---|
| NFR-120 | 새 의존성·마이그레이션·환경변수가 없다 |
| NFR-121 | 기다림 표시는 서버를 부르지 않는다 — 화면의 시계로만 센다 |

---

## D. 설계

### D.1 기다림 (FR-1300 ~ FR-1302)

- 흐름 상태 기계(`apps/web/src/components/llmStream.ts`의 `chatReducer`)에 **`waitingSince`**(보낸 시각, 없으면 `null`)를 둔다. 보낼 때
  시각을 행동에 실어 넣고(`send.at`), 첫 `thinking`·`delta`·`rethink`, `end`, 실패, 대화 옮김에서 비운다. `ping`은 그대로 둔다.
- 표시는 순수 함수 `waitLabel(waitingSince, now)` → `{ seconds, slow }`. 화면은 기다리는 동안만 1초마다 다시 그린다.
- 문구는 사용자가 쓴 그대로 — `답변을 기다리고 있습니다 · 12s`, `답변이 늦어지고 있습니다.` 흘러나오는 답 자리의 맨 위, `role="status"`.

### D.2 순서·개수 (FR-1310 ~ FR-1313)

- 공유 규칙 `NON_EMPTY_NODES`·`FIRST_CHILD`(`packages/shared/src/document.ts`). `validateDocument`가 자식 종류(`ALLOWED_CHILDREN`) 다음에 본다.
- 대조 시험은 편집기 스키마의 내용 자동기계(`ContentMatch`)를 따라가 **첫 걸음은 `FIRST_CHILD`(없으면 허용 자식 전부), 그 뒤는 허용 자식을
  되풀이하는 한 상태, 비어 끝날 수 있음 = `NON_EMPTY`가 아님**을 확인한다. 모양이 다르면(예: 두 번째 자식 규칙) 실패한다.
- 변환(`apps/api/src/pages/domain/ydoc.ts`의 `docFromYDoc`)은 자식을 먼저 읽고, 요소마다 규칙을 대어 어기면 떨어뜨린다 — 편집기(y-tiptap)가
  노드를 만들다 실패한 요소를 지우는 것과 같은 순서다. 맨 위가 비면 빈 문단 하나를 둔다.

### D.3 프레임 (FR-1320·1321)

- `new WebSocketServer({ noServer: true, maxPayload: COLLAB_LIMITS.maxFrameBytes })`. 넘은 프레임은 `ws`가 읽지 않고 오류를 내며 1009로 닫는다 —
  게이트웨이가 그 오류를 가려(`WS_ERR_UNSUPPORTED_MESSAGE_LENGTH`) warn 한 줄을 남긴다.
- 화면의 연결 상태 기계(`collabLink.ts`)는 닫힘 코드 1009를 `refused`로 본다.

### D.4 표 칸의 값 (FR-1322·1323)

- `nodeAttrProblems`의 표 칸 규칙에 `colspan`·`rowspan`(정수·범위)과 `colwidth`(길이·값의 범위)를 더한다.
- 편집기의 `TableCell`·`TableHeader`를 넓혀 `parseHTML`이 값을 범위로 줄인다(`clampSpan`·`clampColwidth` — `extensions.ts`). 셀 병합 명령이 만드는
  값은 표의 칸 수를 넘지 않는다.

### D.5 남는 것

| 무엇 | 왜 남기나 | 자리 |
|---|---|---|
| 칸 없는 줄은 편집기가 가리지만(표 정규화) 공유 문서와 정본에는 남는다 | 스키마가 허락하는 모양이다. 보기 화면에는 빈 표로 그려진다 — 해가 없다 | — |
| 1000칸을 넘는 표를 셀 병합으로 합치면 관문이 끊는다 | 1000열의 표는 정상 편집에서 나오지 않는다 | 보고가 오면 |

---

## E. 데이터 모델

바뀌지 않는다. **옛 데이터**: 개발 DB의 모든 버전·템플릿을 새 정본 검증에 대어 본다 — 검증기록.

## F. API 계약

바뀌지 않는다. 실시간 편집의 닫힘 코드 1009(프레임이 너무 크다)가 더해진다.

## G. 화면

| 화면 | 무엇 |
|---|---|
| LLM 질문 | 흘러나오는 답 자리 맨 위 — `답변을 기다리고 있습니다 · Ns`, 5초부터 `답변이 늦어지고 있습니다.` |
| 편집(실시간) | 프레임이 너무 크면 관문의 거절과 같은 알림 |

## H. 모듈과 등급

| 모듈 | 경로 | 등급 | 하는 일 |
|---|---|---|---|
| 문서 규칙 | `packages/shared/src/document.ts` | **A** | 순서·개수(D.2), 표 칸 값(D.4) |
| 상한 | `packages/shared/src/constants.ts` | **A** | `COLLAB_LIMITS`·`TABLE_LIMITS`·`LLM_TIMINGS.slowAnswerMs` |
| 변환 | `apps/api/src/pages/domain/ydoc.ts` | **A** | 편집기처럼 읽기(D.2) |
| 게이트웨이 | `apps/api/src/pages/collab/collab.gateway.ts` | B | 프레임 상한(D.3) |
| 흐름 상태 기계 | `apps/web/src/components/llmStream.ts` | B | 기다림(D.1) |
| 연결 상태 기계 | `apps/web/src/components/collabLink.ts` | B | 1009(D.3) |
| 편집기 확장 | `apps/web/src/components/extensions.ts` | B | 표 칸 값 줄이기(D.4) |
| 화면 | `apps/web/src/pages/LlmPage.tsx` | B | G절 |

## I. 설정 항목

없다. 설계 고정값: `COLLAB_LIMITS.maxFrameBytes`(16MiB), `TABLE_LIMITS`(`maxSpan` 1000·`maxColWidthPx` 10000), `LLM_TIMINGS.slowAnswerMs`(5000).

## J. 보류 결정 처리

| # | 처리 |
|---|---|
| 30 | **닫는다** — 제한을 두지 않는다(사용자 결정), 기다림을 보인다(C.1) |
| 25 | **닫는다** — 규칙을 정본과 변환에 들이고 대조 시험이 지킨다. 받는 편집기의 동작을 쟀다(C.2) |
| 27 | **닫는다** — 프레임 16MiB, 표 칸 값의 범위(C.3) |
| 16 | 그대로 — 사용자가 뜻을 물었고 정하지 않았다 |

## K. 구현 순서

1. A등급 — 순서·개수 규칙·표 칸 값·상한(shared), 변환(api). **테스트 먼저**
2. 게이트웨이 프레임 상한 · 화면(기다림·1009·표 칸 값 줄이기) — 컴포넌트·상태 기계 시험, 대조 시험
3. E2E — 늦게 오는 답(가짜 LLM이 첫 조각을 6초 늦춘다)에서 초와 "늦어지고 있습니다"가 보이고, 답이 오면 사라진다
4. 옛 데이터 대조, 컨테이너(프레임 상한으로 닫히는 것, 기동)
5. 검증기록 → 검토(자체 점검·코드 리뷰·보안 검토 — 관문·검증을 만진다·문서 정합성) → 학습가이드·장애대응 → PR → **CI** → 병합
